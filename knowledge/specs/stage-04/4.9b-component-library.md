---
type: session-spec
stage: 04
session: "4.9b"
slug: component-library
status: done        # authorized to proceed by the developer ("4.9b is a go") with 4 holds
created: 2026-06-12
updated: 2026-06-12
owner: developer
plan: "knowledge/plans/stage-04/4.9b-component-library.md"
report: "knowledge/steps/stage-04/4.9b-component-library.md"
---

# Session 4.9b — Public component library + §4.3 async/error primitives

## Linked documents
- Umbrella: [[specs/stage-04/4.9-frontend-foundation-platform-hygiene]] (§4.2 contract, §4.3 conventions, §6.4 a11y smoke)
- Predecessor: [[specs/stage-04/4.9a-tokens-tailwind-shell]] (tokens + shell + React Aria/lucide deps + portal roots)
- Design plan: [[design-plan]] (1.2 tokens, 1.4 signature pipeline) · Findings: [[steps/findings-4.9]]
- Spec: [[specs/stage-04/4.9b-component-library]] · Plan: [[plans/stage-04/4.9b-component-library]] · Report: [[steps/stage-04/4.9b-component-library]]
- ADR: [[decisions/adr-047-component-api-contract-stability]] · uses [[decisions/adr-046-component-primitive-strategy]]

## Goal
Build the §4.2 public component library + the §4.3 async/error primitives with stable, token-only
contracts, each shipped **with its static-a11y smoke**, so Stage 5+ builds against them without churn.
Refactor the 4.9a error/404/403 surfaces onto the Empty State component (preserving gate selectors).

## Why now
The highest-leverage deliverable in the stage (umbrella §2.2) — a wobbly Button contract is 200 call-site
edits in Stage 8. Components depend on 4.9a's tokens + React Aria; 4.9c restyles consume these.

## Read first
- `frontend/src/app/globals.css` (the semantic tokens — the ONLY colors components may use)
- `knowledge/specs/stage-04/4.9-frontend-foundation-platform-hygiene.md` §4.2 (the contract table), §4.3, §6.4
- `frontend/src/features/content/lecturer/TranscriptStatusBadge.tsx` (the pipeline the Progress/Step component generalizes — 4.9c rewires it)

## Source paths likely touched
- `frontend/src/components/ui/*` (NEW: components + helpers + `variants.ts` + `cn.ts` + `index.ts` + co-located `*.a11y.test.tsx`)
- `frontend/src/app/error.tsx`, `not-found.tsx`, `(app)/unauthorized/page.tsx`, `components/auth/AccessDenied.tsx` (refactor onto Empty State — selectors preserved)
- `frontend/vitest.config.ts`, `frontend/vitest.setup.ts` (NEW — MINIMAL runner, see scope note)
- `frontend/package.json` (+lock) — Vitest/RTL/axe deps; clsx + tailwind-merge
- `knowledge/decisions/adr-047-*.md` (NEW)

## Build
1. **Foundation:** `cn.ts` (clsx + tailwind-merge); `variants.ts` — variant→**complete literal class strings** (never constructed; the §4.2 purge footgun).
2. **Public components (§4.2 contract — variants/states/a11y exactly as the table):** Button, Input
   (text/textarea/select), Card (+interactive), Badge, Modal (React Aria, via `#modal-root`), Table
   (+sortable), Toast (React Aria provider, via `#toast-root`), Empty State, Progress/Step (linear +
   stepped; **failed state mandatory & visually distinct** — the signature pipeline).
3. **Internal helpers:** Spinner, Skeleton, VisuallyHidden (focus-trap comes from React Aria — no custom util).
4. **§4.3 async/error primitives:** Skeleton (content), inline Button spinner (`aria-busy`), Empty-State-with-retry, the styled error boundary (refactor `error.tsx` onto Empty State).
5. **Client/server boundary:** `"use client"` on Button/Input/Modal/Toast/interactive-Card/sortable-Table; presentational (Badge/EmptyState/linear-Progress/static-Card/helpers) need none.
6. **Co-located static-a11y smoke per §6.4** — written WITH each component (developer hold #3), runnable via the minimal Vitest runner.
7. **Refactor error/404/403/Access-denied onto Empty State** — preserve `getByRole('heading',{name:'Unauthorized'|'Access denied'})` + "Log out" (developer hold #4).
8. **ADR-047** (contract stability: breaking frozen post-4.9 / additive allowed via changelog).
9. **File F-4.9-3** (done in 4.9a; confirm closed-state before 4.9b close — developer hold #1).

## Do not build
- Feature-surface restyle (login/admin/lecturer/student/4.5d–4.7 panels) — 4.9c. No `style={}` removal from feature surfaces.
- The §6.1–6.3 logic tests, the `test:unit`/`test:a11y`/`check:*` scripts, CI workflow, pre-commit hook — **4.9d** (only the minimal a11y runner here; see scope note).
- The hygiene batch (§7/§7.4) — 4.9e. The Stage 8 assistant widget (anchor reserved in 4.9a). Dark mode. Mono font.
- Any backend change. `upload.ts` untouched.

## Scope note — test-harness split (developer hold #3)
4.9b stands up a **minimal Vitest + RTL + jsdom + vitest-axe** runner solely to **execute the co-located
component static-a11y smokes as each component lands** — a component shipped without its contract test is
a contract in prose. The **full gate surface** (the §6.1–6.3 logic tests, `test:unit`/`test:a11y`/
`check:design-tokens`/`check:inline-styles` scripts, GitHub Actions CI, Husky pre-commit) remains **4.9d**.

## Data model / API / Worker / Authz changes
None.

## Verification
- `npx tsc --noEmit` → 0 errors; `npm run build` → clean (literal-class purge guard).
- **Component static-a11y smoke** (§6.4) → green for Button/Input/Badge/Modal/Table/Toast/Progress (+others).
- **Token purity (developer hold #2):** grep `src/components/ui` for `--palette-` or raw hex `#[0-9a-fA-F]{3,8}` → **0 matches** (components use semantic tokens only; React Aria injects behavior, not color).
- **Anti-regression (developer hold #4):** rebuild image; re-run `4.3.5b` (+ `4.3.5c`) `--workers=1` → green; the refactored 403/404/error surfaces keep their headings + "Log out" byte-for-byte; teardown after.
- Build-output size reviewed (§4.4 sanity).

## Done means
- All §4.2 public components built, token-only, literal class strings, correct client/server boundary; helpers as needed.
- Contracts: breaking frozen (ADR-047); additive allowed via changelog. §4.3 primitives shipped.
- Each component has a passing co-located static-a11y smoke (§6.4).
- error/404/403/Access-denied refactored onto Empty State; gate selectors preserved; 4.3.5b/4.3.5c green.
- Token-purity grep clean; tsc + build clean. ADR-047 recorded; F-4.9-3 filed. Trio linked.

## Amendments
_Add dated entries here if scope changes mid-flight._
