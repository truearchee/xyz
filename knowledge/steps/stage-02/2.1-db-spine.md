---
type: session-report
stage: 2
session: "2.1"
slug: db-spine
status: complete
created: 2026-05-29
updated: 2026-05-30 19:24
spec: knowledge/specs/stage-02/2.1-db-spine.md
plan: knowledge/plans/stage-02/2.1-db-spine.md
commit: 0c61593
---
2.1
# Session 2.1 — Report — DB Spine

## Linked documents
- Spec: [[specs/stage-02/2.1-db-spine]]
- Plan: [[plans/stage-02/2.1-db-spine]]
- Report: [[steps/stage-02/2.1-db-spine]]
- Architecture: [[architecture/db-spine]]

## Summary
Completed the schema-only DB spine: five SQLAlchemy models, Alembic metadata wiring, migration `0002_db_spine`, local `pgcrypto` init, isolated DB-spine tests, and knowledge updates. No endpoints, auth logic, service/repository layer, storage integration, seed data, or business logic were added.

## Files changed
- Backend: `backend/pyproject.toml`, `backend/alembic/env.py`, `backend/alembic/versions/0002_db_spine.py`, `backend/app/platform/db/models/`, `backend/tests/test_db_spine.py`
- Infra: `.env`, `.env.example`, `docker/postgres/init/001-create-vector.sql`
- Knowledge: `knowledge/specs/stage-02/2.1-db-spine.md`, `knowledge/plans/stage-02/2.1-db-spine.md`, `knowledge/steps/stage-02/2.1-db-spine.md`, `knowledge/architecture/db-spine.md`, `knowledge/architecture/repo-skeleton.md`, `knowledge/STATUS.md`, `knowledge/log.md`
- Prior-session report change history updated as requested: `knowledge/steps/stage-01/1.1-repo-skeleton.md`
- Pre-existing/unrelated dirty file left untouched: `knowledge/.obsidian/workspace.json` changed outside this session and was not edited.

## Modified prior sessions
- Session 1.1 — `.env`: added `TEST_DATABASE_URL` so the exact Docker pytest commands run DB spine tests instead of skipping them.
- Session 1.1 — local Postgres volume: recreated the database volume so `docker/postgres/init/001-create-vector.sql` installs both `vector` and `pgcrypto` in the live local `xyz_lms` database.

## Verification
| Command | Result | Notes |
|---|---|---|
| `python3 -m compileall backend/app backend/tests backend/alembic` | passed | All touched Python modules compiled. |
| `docker compose build backend worker` | passed | Installed `uuid6-2024.7.10` in rebuilt backend/worker images. |
| `docker compose up -d` | passed | Backend and worker recreated; backend health check passed. |
| `docker compose exec backend alembic upgrade head` | passed | Database was already at head after the prior verification pass; exit 0 confirmed `0002` applied. |
| `docker compose exec backend alembic downgrade base` | passed | Downgraded `0002` then `0001`. |
| `docker compose exec backend alembic upgrade head` | passed | Re-applied `0001` and `0002` after downgrade. |
| `docker compose exec -e TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/xyz_lms_test backend pytest tests/test_db_spine.py` | passed | `3 passed in 1.21s`; tests used isolated `xyz_lms_test`. |
| `docker compose exec -e TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/xyz_lms_test backend pytest` | passed | `4 passed, 1 warning in 1.35s`; warning is existing `httpx` ASGI shortcut deprecation in `test_health.py`. |
| `docker compose exec frontend npx tsc --noEmit` | passed | No output; exit 0. |
| `git diff --check` | passed | No whitespace errors. |
| `docker compose exec backend python -c "from app.platform.db.models import Base; print(sorted(Base.metadata.tables))"` | passed | Metadata contained `app_users`, `course_memberships`, `course_modules`, `module_sections`, `section_assets`. |
| Non-template wikilink resolution script | passed | `all non-template wikilinks resolve`; template placeholders were intentionally excluded. |
| `docker compose down -v` | passed | Destroyed the local Postgres volume so init SQL would run again. |
| `docker compose up -d` | passed | Recreated services and the fresh `test2_postgres_data` volume. |
| `docker compose exec db psql -U postgres -c "CREATE DATABASE xyz_lms_test;"` | passed | `CREATE DATABASE`; isolated test DB exists for exact pytest commands. |
| `docker compose exec db psql -U postgres -d xyz_lms -c "\dx"` | passed | Listed `pgcrypto`, `plpgsql`, and `vector`. |
| `docker compose exec backend alembic upgrade head` | passed | Ran `0001` and `0002` on the recreated database. |
| `docker compose exec backend pytest tests/test_db_spine.py -v` | passed | `3 passed in 1.29s`; no skips. |
| `docker compose exec backend pytest -v` | passed | `4 passed, 1 warning in 1.32s`; no skips or failures. Warning is the existing `httpx` ASGI shortcut deprecation in `test_health.py`. |
| `docker compose exec frontend npx tsc --noEmit` | passed | No output; exit 0. |

## Deviations from spec
None.

## Decisions made
None requiring an ADR. UUIDv7, file_url naming, and service-layer role enforcement were already specified by the approved session spec.

## Risks introduced
- Docker init SQL only applies to newly initialized local containers. Managed/hosted Postgres will need explicit `vector` and `pgcrypto` extension bootstrap before first deployment.
- The full backend suite reports one existing deprecation warning from `httpx` in the Stage 1 health test.

## Follow-ups
- Session 2.2 can build current-user context on top of `app_users.timezone`.
- Before hosted deployment, add an explicit database extension bootstrap path for `vector` and `pgcrypto`.

## Knowledge updates
- Added `knowledge/specs/stage-02/2.1-db-spine.md`.
- Added `knowledge/plans/stage-02/2.1-db-spine.md`.
- Added `knowledge/architecture/db-spine.md`.
- Updated `knowledge/architecture/repo-skeleton.md` to stop claiming no database tables exist.
- Updated `knowledge/STATUS.md`.
- Appended `knowledge/log.md`.

## Close-the-loop checklist
- [x] Spec exists and approved/done
- [x] Plan existed and was approved before coding
- [x] Stayed in scope (deviations noted above)
- [x] Verification commands run; real output recorded
- [x] Report written from git diff/status + command output, not memory
- [x] spec ↔ plan ↔ report links all resolve
- [x] STATUS.md overwritten; log.md appended
- [x] architecture/ updated IF source paths changed
- [x] ADR added IF a durable decision was made
- [x] open-questions.md updated IF anything unresolved

## Change history
_Append-only. One dated line per change made after initial completion._
- 2026-05-29 — initial completion (uncommitted)
- 2026-05-29 19:25 — review failures fixed: `.env` now provides `TEST_DATABASE_URL`; local Postgres volume recreated so `pgcrypto` is installed.
- 2026-05-29 20:56 — [Session 2.2] reused `app_users`, `course_modules`, and `course_memberships` for JWKS-backed `CurrentUserContext` resolution.
- 2026-05-30 00:32 — [Session 2.3] added `supabase` and `pydantic[email]` dependencies for admin provisioning and DTO validation.
- 2026-05-30 19:24 — [Session 3.1] migrated the placeholder `section_assets` table to storage-backed upload metadata with `storage_key`, checksum, uploader, and section/uploader indexes.
