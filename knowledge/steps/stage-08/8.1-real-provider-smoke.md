---
type: real-provider-smoke
stage: 08
session: "8.1"
status: pass
created: 2026-06-18
updated: 2026-06-18
---

# Stage 8.1 — real-provider smoke (rule 11) — PASS

One assistant turn run against the **real** K2 provider (`LLM_PROVIDER=k2think`, live `LLM_API_KEY`)
through the full gateway chain (PromptRegistry → limiter → AIRequestLog → OutputValidator → provenance),
asserting the model-ID echo matches the configured identifier.

## Result (2026-06-18)
- Prompt: `assistant/v1` (model `MBZUAI-IFM/K2-Think-v2`, backend `cerebras`).
- Question: "Briefly, what is a binary search tree and why is it useful for searching?"
- **MODEL_ECHO: `MBZUAI-IFM/K2-Think-v2`** — matches the configured prompt model ✅ (rule 11).
- BACKEND_USED: `cerebras` (requested route).
- USAGE: prompt 316 / completion 551 / total 867 tokens.
- Answer: a real, coherent study-assistant explanation of a BST (markdown), validated by OutputValidator
  (`AssistantAnswer`), no refusal.
- Status: **PASS** on the first attempt.

## Notes
- The smoke was run in a one-off `k2think` backend container (real key injected via env; the workspace's
  committed `.env` carries only the `.env.example` placeholder key). It does not gate CI — CI/E2E use the
  deterministic provider at the boundary.
- This is the first interactive (`priority="interactive"`) real-provider call — rule-15 interactive
  headroom exercised end-to-end.
