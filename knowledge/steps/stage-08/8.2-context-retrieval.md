---
type: session-report
stage: 08
session: "8.2"
slug: context-retrieval
status: complete
created: 2026-06-18
updated: 2026-06-20
spec: knowledge/specs/stage-08/8.2-context-retrieval.md
plan: knowledge/plans/stage-08/8.2-context-retrieval.md
commit: ""
---

# Session 8.2 — Report — Context resolver + grounded retrieval

## Linked documents
- Spec: [[specs/stage-08/8.2-context-retrieval]]
- Plan: [[plans/stage-08/8.2-context-retrieval]]
- Report: [[steps/stage-08/8.2-context-retrieval]]
- Builds on: [[steps/stage-08/8.1-conversation-foundation]] (FULLY VERIFIED)
- ADRs: [[decisions/adr-050-embedding-platform-promotion]], [[decisions/adr-051-assistant-grounding-architecture]]
- Calibration: [[steps/stage-08/8.2-retrieval-threshold-calibration]]
- Smoke (passed): [[steps/stage-08/8.2-real-provider-smoke]]

## Summary
Built Stage 8.2 server-side single-lecture grounding: the embedder is promoted to `platform/embeddings/`
with a shared `EmbeddingConfig` + an `EMBEDDING_PROVIDER` deterministic CI mode (ADR-050); the assistant
worker now runs **resolve → retrieve → call-once → ground → snapshot** (ADR-051) — an exact pgvector
cosine scan scoped to the conversation's STORED section through the 4.7 visibility gate, a deterministic
threshold, a single INTERACTIVE gateway call returning a required structured `isStudyRelated` flag,
backend-derived `groundingStatus` via a fixed-precedence `decide_grounding`, and a server-only
generation-time `context_snapshot` (migration 0033) from which the student-safe "Where did this come
from?" basis is composed. The frontend adds the neutral "Not from this lecture" label + the collapsed
basis disclosure.

**Stage 8.2 FULLY VERIFIED (2026-06-18).** Backend 558 pytest + tsc + migration round-trip + 5 security
tests + focused /cso (0 findings) + 8.2 browser gate + full active Playwright 17/17 (rule 14) + the
real-provider smoke (rule 11) all GREEN — model echo `MBZUAI-IFM/K2-Think-v2`, and the real model emitted
the required `isStudyRelated` flag correctly. See [[steps/stage-08/8.2-real-provider-smoke]].

**Review follow-up state (2026-06-18):** approved summaries now enter grounded prompt context alongside
normalized chunks, Stage 8 E2E includes 375px no-horizontal-scroll proof, and the real-provider smoke is
strict on `isStudyRelated` mismatches. Deterministic gates reran green (backend 560 pytest, `tsc` exit 0,
focused 8.2 gate, full active Playwright 17/17). Strict real-provider smoke reran green with a real key
injected via env only: model echo `MBZUAI-IFM/K2-Think-v2` matched on both turns and `isStudyRelated`
matched expected true study / false off-topic.

## Files changed
**Backend — new:** `alembic/versions/0033_assistant_message_context_snapshot.py`;
`platform/embeddings/{__init__,config,encoder}.py`; `platform/query/assistant_retrieval_read.py`;
`domains/assistant/{config,grounding}.py`; `prompts/assistant/v2.yaml`;
`scripts/gate8_assistant_smoke.py`; `tests/{test_assistant_grounding,test_embedding_platform}.py`.
**Backend — modified:** `domains/assistant/generation_service.py` (grounding pipeline) +
`service.py` (snapshot→safe basis, context_unavailable retry); `platform/config.py`
(`EMBEDDING_PROVIDER`); `platform/llm/{models/assistant,validation,gateway,provider}.py`
(`AssistantGroundedAnswer` + validator branch + unions + deterministic v2 fixture/marker/fault);
`platform/db/models/assistant_message.py` (`context_snapshot` JSONB);
`domains/transcripts/embedding_service.py` + `workers/worker.py` (import the promoted encoder via
`get_encoder()`); `prompts/CHECKSUMS.json` (`assistant/v2`); `domains/admin/dev_reseed.py`
(EXPECTED_ALEMBIC_VERSION 0032→0033); `tests/conftest.py` (session deterministic-embeddings) +
4 transcript test files (import path). **Removed:** `domains/transcripts/embedding_encoder.py` (promoted).
**Frontend — modified:** `features/assistant/AssistantPanel.tsx` (label + basis disclosure). No client
regen — `MessageRead` already exposed `groundingStatus` + `answerBasis`.
**Tests/fixtures:** `tests/e2e/8.2-assistant-grounding.spec.ts` (new); `tests/e2e/fixtures/db.mjs`
(`getFirstTranscriptChunkText`, `getAssistantMessageGrounding`).
**Knowledge:** this report + plan + calibration + 2 ADRs + real-provider smoke evidence + gate-image
contention finding.

## Verification
| Command / gate | Result | Notes |
|---|---|---|
| migration 0033 round-trip | passed | upgrade→downgrade(-1)→upgrade; `context_snapshot` appears/drops/reappears; `alembic heads` single `0033` |
| `pytest` (full backend) | **558 passed** | 544 (post-embedder-promotion) + 14 new grounding tests; embedder promotion byte-identical (99 transcript consumer tests green) |
| embedder promotion regression | passed | `test_embedding_platform.py` (config↔CHECK parity, distance contract, get_encoder, prod-guard) + full suite |
| `tsc --noEmit` (frontend) | exit 0 | label + basis disclosure |
| security tests (§13) | passed | injection ×2 (scoped scan), non-vacuous raw-transcript canary, malformed-output fail-safe, ownership/tamper, one-AIRequestLog-row-per-answered-turn, context_unavailable no-row |
| focused **/cso** pass | **clean** | 3 independent verifiers, 12 threats adjudicated, 0 findings ≥8/10; report `.gstack/security-reports/2026-06-18-145302.json` |
| retrieval threshold calibration | done | real MiniLM: in-lecture 0.17–0.21, off-lecture 0.89–0.95; threshold 0.35 (review #8) |
| **8.2 browser gate (live)** | **passed** | grounded lecture + safe basis; off-lecture "Not from this lecture"; unrelated redirect; LAB grounded ("this lab's context"); unassigned 404; DB grounding-status order asserted |
| **full active Playwright suite (rule 14)** | **17/17 passed (4.0m)** | real-MiniLM e2e stack; all prior-stage gates green incl. 4.4-embedding-browser + 4.3.5e (see the timing-race resolution below) |
| **real-provider smoke (rule 11)** | **PASS** | `scripts/gate8_assistant_smoke.py`; model echo `MBZUAI-IFM/K2-Think-v2` (both turns); real model emitted valid `isStudyRelated` (true study / false off-topic); 1147 + 818 tokens; see [[steps/stage-08/8.2-real-provider-smoke]] |

## Review follow-up verification
| Command / gate | Result | Notes |
|---|---|---|
| `docker compose ... exec -T backend pytest` | **560 passed** | includes 16 assistant-grounding tests; summary-context regressions green |
| `docker compose ... exec -T frontend npx tsc --noEmit` | exit 0 | no output |
| focused `tests/e2e/8.2-assistant-grounding.spec.ts --workers=1` | **1 passed (46.0s)** | run id `e2e-1781802463-stage82focus2`; basis + 375px proof green |
| full active Playwright suite | **17/17 passed (5.6m)** | run id `e2e-1781802524-stage82fullrerun`; Stage 8.1/8.2 mobile proof included |
| strict `scripts/gate8_assistant_smoke.py` with `LLM_PROVIDER=k2think` | **PASS** | model echo `MBZUAI-IFM/K2-Think-v2` OK on both turns; `isStudyRelated` true/false both matched; key env-only |

## Live-gate path (root-caused 2 transient reds, both fixed — see [[steps/stage-08/findings-8.2-gate-image-contention]])
1. First full-suite run 15/17: `8.2-assistant-grounding` red — Conductor re-upped `ai_worker` mid-suite
   onto baked non-8.2 code (the 8.1 image-tag hazard; my source-mount override doesn't survive
   Conductor's base-compose re-up). Fixed: baked my code into a UNIQUE image `kyiv-backend-e2e-hatyai`
   and folded the e2e config (unique image + `.env.e2e`) into the base `docker-compose.yml` Conductor
   re-ups with. LOCAL compose edits — REVERT before commit.
2. Second run 16/17: `4.3.5e-transcript` red (also failed in isolation, no restart) — a TEST-TIMING race:
   deterministic embeds + deterministic LLM summaries complete so fast the transcript reaches terminal
   `summarized` before the browser polls the intermediate `chunked` projection state. Product correct (DB:
   all steps completed). Fixed: the live e2e stack uses **real MiniLM** (deterministic for identical text
   → 8.2 still grounds on a verbatim chunk; slow enough to preserve the Stage-4 pipeline timing). The
   deterministic encoder stays scoped to backend pytest. Third run: **17/17 green.**

## Deviations from plan
- One additive migration `0033` (`context_snapshot` JSONB) was required — the review assumed an existing
  message-metadata facility; 0032 added none. Recorded in the plan §2; single head moves 0032→0033.
- Review follow-up closed the approved-summary drift: grounded prompts now pack eligible generated
  summaries alongside normalized retrieved chunks, after the stored-section visibility gate and within
  per-source + total caps.

## Decisions made
- **ADR-050** — promote the embedder to `platform/embeddings/` + shared `EmbeddingConfig` +
  `EMBEDDING_PROVIDER` deterministic-CI mode (provenance parity keeps 4.4 green).
- **ADR-051** — grounding architecture: exact-scan retrieval through the 4.7 gate, backend-derived
  `groundingStatus` (fixed precedence, redirect-before-match), generation-time snapshot → safe basis.

## Risks
- **R-isStudyRelated (MVP-accepted):** the study-relatedness flag is model-judged; the threshold +
  decision order are the deterministic backbone. The real-provider smoke now hard-fails model
  true/false drift as well as missing/invalid flags.
- Single `ai_worker` head-of-line (carried from 8.1); the query embedding adds a local encode (fast under
  deterministic; first real MiniLM query in `ai_worker` pays a one-time model load).

## Modified prior sessions
- **Stage 4.4** — extracted `embedding_encoder` → `platform/embeddings/`; updated `embedding_service.py`,
  `workers/worker.py`, and 5 transcript test files to the new import; behavior byte-identical (compat
  test + 99 consumer tests + full suite green). See ADR-050.
- **Stage 5.5d** — `dev_reseed.py` EXPECTED_ALEMBIC_VERSION 0032→0033.
- **Stage 6** — `prompts/CHECKSUMS.json` +`assistant/v2`.
- **Stage 5b/6a/8.1** — additive `platform/llm/*` widening for `AssistantGroundedAnswer` (no existing
  behavior changed).

## Environment / coordination notes
- Live gate env (local-only, NOT committed): `docker-compose.override.yml` (source mount neutralizing the
  shared `kyiv-backend` tag contention with sibling `tokyo` + `EMBEDDING_PROVIDER=deterministic` on all
  backend services); excluded via `.git/info/exclude`. Supabase healthy (edge_runtime already disabled
  per the 8.1 finding). Ran on the base compose + e2e overlay + override, `COMPOSE_PROJECT_NAME=hat-yai`,
  frontend `:3001`, `--workers=1`.
- ADR numbering collides across lanes (047/048 doubled by Stage 7/9); used descriptive slugs 050/051.
- **Do NOT commit** the 16 incidental `knowledge/steps/stage-06/screenshots/*.png` working-tree changes
  (not produced by 8.2; present from another lane/session) — stage only the 8.2 files.

## Follow-ups
- Re-point `0033` `down_revision` onto the live head before the PR if main moved (currently 0032).
- Carried: dedicated `ai_interactive` worker; promote `require_student`/`require_visible` to `platform/auth`.

## Close-the-loop checklist
- [x] Spec + plan approved before coding
- [x] Stayed in scope (deviations noted)
- [x] Backend verification commands run; real output recorded (558 pytest, round-trip, tsc)
- [x] Security tests (§13) + focused /cso pass — clean
- [x] 8.2 browser gate GREEN (5 outcomes + lab + basis)
- [x] **Full active Playwright suite (rule 14) — 17/17 GREEN** (real-MiniLM e2e stack)
- [x] **Real-provider smoke (rule 11) — PASS** (model echo + real `isStudyRelated` emission)
- [x] ADRs (050, 051), calibration note, smoke + gate-image-contention findings written
- [x] STATUS/log updated; open-questions updated (smoke item resolved)
- [x] **roadmap Stage 8 line + STATUS flipped to "8.2 FULLY VERIFIED"**

## Change history
- 2026-06-18 — built + backend-verified (558 pytest) + /cso clean + 8.2 browser gate green + threshold
  calibrated. Live gate: two transient full-suite reds root-caused + fixed (image contention → unique
  baked image; deterministic-embed timing race → real-MiniLM e2e). **Full active suite 17/17 GREEN.**
- 2026-06-18 — env owner provided a real K2 key; **real-provider smoke PASS** (model echo
  `MBZUAI-IFM/K2-Think-v2`; real model emitted a valid `isStudyRelated`, true study / false off-topic).
  **Stage 8.2 FULLY VERIFIED;** STATUS + roadmap Stage 8 line flipped.
- 2026-06-18 — review follow-up: added approved summaries to grounded prompt context alongside
  normalized chunks, made the real-provider smoke hard-fail `isStudyRelated` mismatches, added 375px
  no-horizontal-scroll proof to Stage 8 browser gates, and repaired linked-doc/open-question closeout.
  Deterministic gates reran green (backend 560, tsc, focused 8.2, full active Playwright 17/17); strict
  real-provider smoke reran green with model echo + `isStudyRelated` true/false enforced.
- 2026-06-20 23:12 — [Session 10] updated `backend/app/domains/student_summaries/service.py` to emit the
  Stage 10 `studied_section` engagement event on the real student section-detail read path via
  `platform/events.record_studied_section`; assistant/context retrieval behavior unchanged.
