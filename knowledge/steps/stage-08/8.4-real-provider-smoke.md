---
type: report
stage: 08
session: "8.4c"
created: 2026-06-19
updated: 2026-06-19
status: pass
---

# Stage 8.4 — rule-11 real-provider smoke — PASS

## Result: PASS (2026-06-19, local, against the reissued K2 key)
Ran `backend/scripts/gate8_assistant_smoke.py` with `LLM_PROVIDER=k2think` and the real key (synced
file-to-file from the operator reference `.env`; never printed). The smoke makes REAL authenticated K2
calls on the **assistant/v2** route (the exact gateway transport) with a synthetic grounded scenario,
validates with the production OutputValidator, and asserts rule 11 (echoed model id == configured id).

```
Stage 8.2 — grounded assistant real-provider smoke (synthetic context; secrets REDACTED)

--- assistant/v2 [study (grounded context)] (route cerebras) ---
  response model echo : MBZUAI-IFM/K2-Think-v2  (expected MBZUAI-IFM/K2-Think-v2)  -> OK
  finish_reason       : 'stop'
  elapsed             : 8.4s
  usage               : prompt=603 completion=586 total=1189
  parseable           : YES (AssistantGroundedAnswer, answer 312 chars)
  isStudyRelated      : True  (expected True)  -> OK

--- assistant/v2 [off-topic] (route cerebras) ---
  response model echo : MBZUAI-IFM/K2-Think-v2  (expected MBZUAI-IFM/K2-Think-v2)  -> OK
  finish_reason       : 'stop'
  elapsed             : 3.6s
  usage               : prompt=602 completion=239 total=841
  parseable           : YES (AssistantGroundedAnswer, answer 143 chars)
  isStudyRelated      : False  (expected False)  -> OK

PASS: assistant/v2 returned the configured model id (rule 11) and expected isStudyRelated judgments.
```

## Why this is the right 8.4 smoke
8.4 (Option A) adds **no new AI surface** — every answer is an existing 8.2 grounded answer reached
through a new entry point. The assistant/v2 route is unchanged, so this smoke proves the REAL model
still works on the path the new Workspace/widget entry points drive, with the model-ID echo asserted
(rule 11). Run: `docker compose run --rm -e LLM_PROVIDER=k2think -T backend python scripts/gate8_assistant_smoke.py`.

## Key handling
The key was read from env and NEVER printed (the script redacts; the value was synced file-to-file). The
operator should confirm the previously-leaked key is **revoked** (not merely replaced) at the provider.

## Second external-review rerun (2026-06-19)
The strict rule-11 smoke reran after the second review fixes with `.env` + `.env.e2e` loaded and
fault-injection env cleared. Result: **PASS**.

```
--- assistant/v2 [study (grounded context)] (route cerebras) ---
  response model echo : MBZUAI-IFM/K2-Think-v2  (expected MBZUAI-IFM/K2-Think-v2)  -> OK
  finish_reason       : 'stop'
  elapsed             : 5.5s
  usage               : prompt=603 completion=381 total=984
  parseable           : YES
  isStudyRelated      : True  (expected True)  -> OK

--- assistant/v2 [off-topic] (route cerebras) ---
  response model echo : MBZUAI-IFM/K2-Think-v2  (expected MBZUAI-IFM/K2-Think-v2)  -> OK
  finish_reason       : 'stop'
  elapsed             : 3.6s
  usage               : prompt=602 completion=239 total=841
  parseable           : YES
  isStudyRelated      : False  (expected False)  -> OK

PASS: assistant/v2 returned the configured model id (rule 11) and expected isStudyRelated judgments.
```
