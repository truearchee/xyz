---
stage: "8.5"
title: Save-to-Glossary from the Assistant — implementation report
status: fully-verified
updated: 2026-06-20
---

# Steps — Stage 8.5 (Save-to-Glossary from the Assistant)

Written from `git status`/`git diff` + captured command output. Branch `stage-8.5-implementation`.

## What was built

**Backend (8.5a)** — extends the ONE Stage 7 write path; no Stage 7 redesign.
- `backend/alembic/versions/0041_glossary_conversation_source.py` (NEW): adds `source_conversation_id`
  → `assistant_conversations` and `source_message_id` → `assistant_messages` (nullable, SET NULL) to
  `glossary_source_references`; drop+recreate the `source_type` CHECK to add `'conversation'`; partial-
  unique index `uq_glossary_source_references_conversation_message (glossary_entry_id, source_message_id)
  WHERE source_type='conversation'`. Guarded/re-runnable; downgrade deletes `'conversation'` rows first.
- `backend/app/platform/db/models/glossary_source_reference.py` (M): the two FK columns, widened CHECK,
  the partial-unique `Index` in `__table_args__` (predicate identical to the migration).
- `backend/app/domains/glossary/schemas.py` (M): `ConversationSaveSource`; `SaveHighlightRequest`
  gains optional `conversation` + optional `module_section_id` + a `model_validator` requiring exactly
  one; `GlossarySourceReferenceRead` gains the two FK fields.
- `backend/app/platform/query/assistant_save_source_read.py` (NEW): `get_conversation_save_source(...)`
  → `ResolvedConversationSource` or `None` (pinned-404 set). Reuses `get_visible_student_section`; does
  NOT import the assistant domain (rule 8).
- `backend/app/domains/glossary/snippet.py` (NEW): conservative `normalize_for_containment` +
  `selected_text_in_message` (anti-spoofing).
- `backend/app/domains/glossary/save_service.py` (M): `save_term` gains `definition_context` +
  `source_conversation_id` + `source_message_id`; `_resolve_definition` decouples prompt context from the
  stored snippet; `_add_source_reference` helper makes the conversation source-attach idempotent
  (ON CONFLICT DO NOTHING) on both new-entry and duplicate paths; summary/manual/quiz behavior unchanged.
- `backend/app/domains/glossary/service.py` (M) + `policy.py` (M): `_save_from_conversation`
  orchestration (require_student → selectedText required 422 → read model None→404 → role!=assistant 422
  → status!=completed 409 → containment 422 → subject from section → `save_term(..., source_type=
  'conversation', definition_context="")`); five new error-code constants. Summary branch byte-for-byte.
- `backend/app/domains/admin/dev_reseed.py` (M): `EXPECTED_ALEMBIC_VERSION` 0040→0041.
- `backend/tests/test_glossary_conversation_save.py` (NEW): 16 tests (cases a–m).

**Frontend (8.5b)** — single mount point, backward-compatible.
- `frontend/src/features/glossary/SaveToGlossary.tsx` (M): optional `source` prop; sends `conversation`
  when present, else the existing `moduleSectionId`. A11y unchanged.
- `frontend/src/features/assistant/ConversationView.tsx` (M): threads `conversationId` + `saveSectionId`
  → `MessageBubble` → `AssistantAnswerBody`; wraps the completed-assistant `SummaryMarkdown` in
  `<SaveToGlossary>` only when both are set. The single mount point for inline panel + workspace + widget.
- `AssistantPanel.tsx`, `workspace/AssistantWorkspaceConversation.tsx`, `widget/WidgetDrawer.tsx` (M):
  pass `conversationId` + the conversation's `attachedSectionId`.
- `frontend/src/features/assistant/ConversationView.test.tsx` (NEW): 5 affordance-gating component tests.
- `frontend/src/lib/api/**` (regenerated): `ConversationSaveSource`, `SaveHighlightRequest` (+ optional
  `conversation`, optional `moduleSectionId`), `GlossarySourceReferenceRead` (+ FK fields), index.

**Gate** — `tests/e2e/8.5-assistant-save-to-glossary.spec.ts` (NEW): the browser gate incl. the two
negative assertions; uses the seeded standing `student2_e2e` for the cross-student negative.

## Verification (captured)
- Migration chain to a fresh DB: `…0040 -> 0041`; `alembic heads` → `0041 (head)`.
- Migration round-trip: `downgrade 0040` → `upgrade head` clean; single head `0041`.
- Backend `pytest -q`: **619 passed, 1 failed → fixed → green**. The single failure was the
  `dev_reseed` version-pin guard (expected 0040, found 0041) — bumped to 0041; `tests/test_dev_reseed.py`
  3 passed. New `test_glossary_conversation_save.py` **16 passed**; `test_glossary_save.py` (summary
  regression) **9 passed, untouched**. Net backend **620 passed** initially (604 prior + 16 new); **625
  passed** after the review hardening (+1 term-spoof, +4 snippet unit).
- Client regenerated from the live OpenAPI (`SaveHighlightRequest.conversation` + `ConversationSaveSource`
  confirmed in-spec); only the 4 expected client files changed.
- Frontend `tsc --noEmit` exit 0; `test:unit` **9 passed** (incl. 5 new ConversationView tests);
  `test:a11y`/vitest green.
- E2E spec: `npx playwright test --list` → discovers `8.5 assistant save-to-glossary browser gate`;
  full suite lists **21 tests in 18 files** (8.5 added cleanly).
- Rule 11: **no real-provider smoke required** — ADR-055 D1 ships no new AI behavior (empty-context chat
  save shares the manual-add cache row + input hash). Conclusion recorded.

## Deviations / notes
- `module_section_id` made optional on `SaveHighlightRequest` — backward-compatible for existing summary
  callers (they always send it); the validator enforces exactly-one. Contract change → client regen (rule 3).
- `check:design-tokens` / `check:inline-styles` are **known-red** across 396 pre-existing inline-idiom
  violations (quiz/metadata/assistant/glossary — the Stage 12 consistency-sweep backlog, design-plan
  Part 3). My edits added **zero** new inline-style/raw-hex violations; not a stage-close gate (8.4
  shipped against the same baseline).
- Accidentally overwrote then deleted the tracked `backend/openapi.json` (a stale Stage-7 snapshot the
  live client-gen does not consume; 8.1–8.4 left it stale) while dumping the spec for codegen — restored
  to HEAD (`git checkout`). No change to it in this session.

## Live gate — GREEN (2026-06-20, ran LOCALLY on a clean DB)
After the owner delivered the local creds (`.env` + `.env.e2e` copied disk-to-disk from the `kyiv`
sibling — proven-current from the 8.4 gate; auth identical across all siblings), the gate ran on a
non-disruptive alt-port stack recreated from the documented 8.4 recipe (`.context/8.5-gate.override.yml`:
backend + workers on :8005 from the freshly-rebuilt `kyiv-backend` image content-hash-verified to HEAD,
NO source bind-mount; `kyiv-frontend` image — byte-identical package-lock to worcester — running
`next dev` in the node:20 container on :3005 with worcester source bind-mounted; local Supabase :54321;
`LLM_PROVIDER=deterministic`). Clean DB each run (`down -v` → migrate `0040→0041` single head → seed
standing users incl. `student2_e2e`). Prewarmed cold next-dev role routes (the 8.4 gotcha).
- **8.5 browser gate** `tests/e2e/8.5-assistant-save-to-glossary.spec.ts` **PASSED** (happy path + studyable
  + duplicate-idempotent + unbound 404 + cross-student 404 + both negative affordance assertions).
- **Full active Playwright suite 21/21 PASSED** (rule 14, serial `--workers=1`, clean DB, run id
  `e2e-mqlw0xei-9d1e2ebc`, 11.4m) — every prior gate (4.3.5b/c/e, 4.4, 4.5d×3, 4.6d×2, 4.7×2, 5.5e, 5d,
  6d, 7, 8.1, 8.2, 8.4×2, 9) plus 8.5.
- **/qa** real-browser drive (gstack browse) of the save-from-chat flow: CLEAN, 0 bugs, no console errors;
  confirmed the affordance shows on the completed assistant reply and NOT on the user message, highlight
  enables the save, and save returns the correct status (`duplicate` → "Already in your glossary"); DB
  entry is `source_type='conversation'` with both FKs + a generated definition. Screenshot in
  `.gstack/qa-reports/`.
- **One issue caught + fixed (thin UI wiring, as predicted):** the gate's `selectTextIn`/button selectors
  were page-wide, but the lecture page renders the summary SaveToGlossary AND the chat's (shared testids),
  so they grabbed the summary's affordance. Scoped every chat-save interaction to the completed assistant
  reply (commit `011a635`); **product code unchanged**. Re-ran → green.
- **No rule-11 smoke** (D1/ADR-055 — deterministic adapter at the provider boundary; the gate exercises the
  full path incl. AIRequestLog on a cache miss). **8.5 is FULLY VERIFIED.**

## Modified prior sessions
- Session 8.1/8.4 — `frontend/src/features/assistant/ConversationView.tsx`,
  `AssistantPanel.tsx`, `workspace/AssistantWorkspaceConversation.tsx`, `widget/WidgetDrawer.tsx`: added
  the save-to-glossary mount + the two threaded props. Behavior otherwise unchanged.
- Session 7a — `frontend/src/features/glossary/SaveToGlossary.tsx`, `backend/app/domains/glossary/{schemas,
  save_service,service,policy}.py`, `backend/app/platform/db/models/glossary_source_reference.py`:
  generalized the save path + component to the `conversation` source (additive, backward-compatible).
- Session 8.4 — `backend/app/domains/admin/dev_reseed.py`: head pin 0040→0041.

## Follow-ups
- Run the live 8.5 browser gate + rule-14 full suite in the local harness; flip to FULLY VERIFIED.
- Recommended skills before close (per spec): `/cso` (anti-spoofing / ownership / visibility / no
  glossary poisoning), `/review` (F-4.6-class seam), `/qa` against the gate.
- Optional: surface the originating conversation as a clickable source on the glossary entry detail
  (the new FK fields are exposed on `GlossarySourceReferenceRead`); not required (spec Deferred).

## Pre-landing review (2026-06-20, before merge)
- **`/cso` — CLEAN (0 findings at the 8/10 gate)**, matching the 8.2 bar. Verified A01 access control:
  ownership/visibility/binding/soft-delete all collapse to the pinned 404; role/status/text are distinct
  4xx (no 403 leak); the read model imports zero assistant-domain code (rule 8); the conversation save
  feeds empty definition context (no poisoning); no f-string SQL or message-content logging. Report at
  `.gstack/security-reports/`. One accepted tradeoff (markdown normalizer, 3/10 — below gate).
- **`/review` (Claude adversarial subagent + Codex gpt-5.5 cross-model)** — the two models disagreed
  (Claude: ship as-is, having verified the ON-CONFLICT predicate ↔ migration ↔ ORM index byte-identical
  and the migration round-trip at the DB level; Codex: block). Adjudicated against the spec's anti-spoofing
  lock: **one finding actioned** — the persisted `term` was not tied to the message (real `selectedText` +
  arbitrary `term`). Fixed: `_save_from_conversation` now also requires `term`-in-message
  (`GLOSSARY_TERM_NOT_IN_MESSAGE`, 422) + `test_arbitrary_term_with_real_selected_text_rejected` + a direct
  `test_glossary_snippet.py` unit suite (4 tests). ADR-055 D4 amended. Re-verified: 30 glossary tests pass
  (17 conversation-save + 4 snippet + 9 summary regression); full backend suite green.
- **Non-blocking, considered + dismissed/deferred (evidence in the review):** TOCTOU between read-model
  check and `save_term` write (microsecond same-transaction window; FP rule 7 + consistent personal-glossary
  behavior); validator alias-collision (not exploitable — the conversation path never reads
  `module_section_id`); normalizer marker-fuzz (ADR-055 accepted, now pinned by the snippet test); downgrade
  data-loss (documented); affordance shows on `general_not_from_lecture`/`context_unavailable` replies
  (the spec doesn't exclude them — owner's call, 1-line FE change if undesired); widget `?? sectionId` vs
  workspace `?? null` fallback (benign — widget lecture-mode is always bound); entry-type not user-editable
  in the component (pre-existing Stage 7 limitation, defaults to "term", not an 8.5 regression).
- **Operational gotcha (recorded):** the baked `kyiv-backend` worker image carried a stale copy of the
  conversation-save test (the pre-fix single-student version) → a spurious `1 failed` if pytest runs from
  the image instead of the working tree. The committed test passes 16/16 (now 17) from the tree. Rebuild
  the image before the gate (the gate recipe already requires content-hash-verifying the image vs HEAD).

## Change history
- 2026-06-19 — Initial implementation (8.5a backend + 8.5b UI); backend + tsc + unit/component verified;
  live browser gate handed off to the local harness.
- 2026-06-20 — Pre-landing `/cso` (clean) + `/review` (cross-model). One anti-spoofing hardening applied
  (term-in-message); ADR-055 D4 amended; snippet unit tests added. Backend re-verified green.
- 2026-06-20 — Live gate ran GREEN on a clean DB (owner delivered local creds from the `kyiv` sibling):
  8.5 browser gate + full active Playwright **21/21** + `/qa` clean. One test-wiring fix (selector
  scoping, `011a635`). **Flipped to FULLY VERIFIED**; roadmap/STATUS/log updated same commit (rule 12).
- 2026-06-20 — Docs-only pre-merge reconciliation after accepted P2 review finding: confirmed the summary
  `<SaveToGlossary>` path also omits `entryType`, so assistant saves defaulting to `"term"` is a
  pre-existing shared save-flow gap, not an 8.5 regression. Recorded the entry-type picker as deferred to
  the post-MVP watchlist. No code change; no gate rerun; 8.5 remains FULLY VERIFIED.
- 2026-06-20 23:12 — [Session 10] advanced `backend/app/domains/admin/dev_reseed.py`
  `EXPECTED_ALEMBIC_VERSION` from `0041` to `0081` for the Stage 10 migration head; 8.5 behavior unchanged.

## Linked documents
- Spec: [[specs/stage-08/8.5-save-to-glossary]]
- Plan: [[plans/stage-08/8.5-save-to-glossary]]
- ADR: [[decisions/adr-055-conversation-sourced-glossary-save]]
- Findings: [[steps/stage-08/findings-8.5-gate-handoff]]
- Roadmap: [[roadmap]]
