---
type: real-provider-smoke
stage: 08
session: "8.2"
status: pass
created: 2026-06-18
updated: 2026-06-18
---

# Stage 8.2 — real-provider smoke (rule 11) — PASS

Two `assistant/v2` (grounded) turns run against the **real** K2 provider (`LLM_PROVIDER=k2think`, live key
injected via env only — never committed) through the full gateway chain (PromptRegistry → limiter →
AIRequestLog → OutputValidator → provenance), asserting the model-ID echo AND that the real model emits
the REQUIRED structured `isStudyRelated` flag (the one 8.2-specific risk, R-isStudyRelated).

Command (operator shell, real key exported, never written to a file):
```
docker compose run --rm -e LLM_PROVIDER=k2think -e LLM_API_KEY="$LLM_API_KEY" -T backend \
  python scripts/gate8_assistant_smoke.py
```

## Result (2026-06-18) — PASS
Prompt `assistant/v2` (model `MBZUAI-IFM/K2-Think-v2`, backend `cerebras`, requested route).

| case | model echo | isStudyRelated (expected) | parseable | finish | tokens |
|---|---|---|---|---|---|
| study (grounded context) | `MBZUAI-IFM/K2-Think-v2` ✅ | **True** (True) ✅ | AssistantGroundedAnswer, 260 chars | stop | 542/605/1147 |
| off-topic ("what movie…") | `MBZUAI-IFM/K2-Think-v2` ✅ | **False** (False) ✅ | AssistantGroundedAnswer, 191 chars | stop | 541/277/818 |

- **MODEL_ECHO matches the configured identifier** on both calls — rule 11 holds (no silent deployment
  alias). HARD gate PASS.
- **The real K2-Think-v2 emitted a valid `{answer, isStudyRelated}` for both turns**, and judged the flag
  correctly (true for the study question, false for off-topic). The R-isStudyRelated risk (model-judged
  flag) is validated end-to-end on the real model — not just the deterministic double.
- `finish_reason='stop'` on both (complete, not truncated); first call 8.2s, second 4.1s.

## Notes
- The key was passed ONLY via `-e LLM_API_KEY` to a one-off `k2think` container; it is in no committed
  file, no `.env`, and no knowledge artifact (the committed `.env` keeps the `.env.example` placeholder).
- First attempt mis-passed the key (a shell inline-assignment vs same-line `$VAR` expansion bug → empty
  key → the script's pre-check failed before any provider call); re-run with the key `export`ed → PASS.
- This is the second interactive (`priority="interactive"`) real-provider feature; the assistant runs at
  INTERACTIVE priority (rule 15). One gateway call per answered turn (review #6) holds.
