---
type: step-report
stage: "04"
session: "4.5c"
slug: detailed-real-provider-smoke
status: deferred-pending-key-rotation
created: 2026-06-11
spec: knowledge/specs/stage-04/4.5c-detailed-summary-generation.md
---

# Stage 4.5c — Detailed-Summary Real-Provider Smoke (synthetic transcript only)

**Status: DEFERRED to the operator** — same discipline as 4.5b §14. Requires the rotated IFM key
(§0 of 4.5b). The agent never holds, prints, commits, or prompts with the key. CI stays on the
`DeterministicTestProvider`.

## Preconditions (operator)
```
- IFM key ROTATED; new key exported, never committed.
- export LLM_PROVIDER=k2think
- export LLM_API_KEY=<rotated key>
- export LLM_PROVIDER_BASE_URL=https://api.k2think.ai
- export ENABLE_DETAILED_SUMMARY=true        # 4.5c default; explicit for the smoke
- export LLM_CONTEXT_FALLBACK_ENABLED=false   # single-model deviation (ADR-025)
- LLM_PROVIDER_JSON_MODE: true only if the 4.5b brief smoke already confirmed response_format is honored.
```

## What it must prove (not merely a 200)
The **detailed** path returns a **parseable `DetailedSummary` with all required sections**, through the
full gateway → provider (Nvidia route) → validator → stored `contentJson`.

## Procedure (synthetic data only)
```
1. Render detailed_summary/v1 against a SHORT SYNTHETIC transcript fixture (include a 'lab'-typed
   variant to exercise the labNotes-required path, and a 'lecture' variant where labNotes is optional).
2. LLMGateway.complete(prompt_key=detailed_summary/v1, output_schema=DetailedSummary,
   priority=background, feature=summary_detailed) with a throwaway ingestion_job row.
3. Assert: result["parsed"] is a DetailedSummary with every required section present and non-empty;
   AIRequestLog row status=succeeded, backend_used=nvidia, backend_route_source=requested,
   providerAttemptCount=1. The request carried metadata.use_nvidia=true (Nvidia route).
```

## Evidence to record here (redacted)
```
- response model echo:        MBZUAI-IFM/K2-Think-v2   (rule-11; expected)
- usage:                      prompt_tokens=__  completion_tokens=__  total_tokens=__
- response_format honored:    yes / no
- backend_used / route_source: nvidia / requested
- reasoning_content:          present-but-null (→ reasoning_level null, never faked; F-4.5-04)
- estimate-vs-actual:         estimated=__ actual=__ observedCharsPerToken=__
- parseable DetailedSummary:  yes — all required sections non-empty; labNotes present for the lab fixture
- Authorization header:       REDACTED
```

## Outcome
- [ ] Recorded by operator on <date>.
- No real student/uploaded/production transcript is sent to IFM in 4.5c (synthetic only).
- 4.5c does **NOT** mark Stage 4.5 FULLY VERIFIED (that is 4.5d). Dual-model gate-2.B (real Think-v0)
  remains deferred on access (F-4.5-27).
