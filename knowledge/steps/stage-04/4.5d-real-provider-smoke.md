---
type: step-report
stage: "04"
session: "4.5d"
slug: full-stage-real-provider-smoke
status: deferred-pending-key-rotation
created: 2026-06-11
spec: knowledge/specs/stage-04/4.5d-lecturer-summary-ui-and-stage-close.md
---

# Stage 4.5d — Full-Stage Real-Provider Smoke (Half 2; the FULLY VERIFIED gate)

**Status: DEFERRED to the operator.** This is the gate that flips Stage 4.5 to FULLY VERIFIED. Until
it is recorded, the stage stays NOT FULLY VERIFIED regardless of Half-1 completion (§0/§7/§11). The
agent never holds, prints, commits, or prompts with the key.

## Preconditions (operator)
```
- IFM key ROTATED; exported as LLM_API_KEY; never committed.
- export LLM_PROVIDER=k2think
- export LLM_API_KEY=<rotated key>
- export LLM_PROVIDER_BASE_URL=https://api.k2think.ai
- ENABLE_DETAILED_SUMMARY=true (4.5c default)
- LLM_CONTEXT_FALLBACK_ENABLED=false (single-model deviation, ADR-025)
- Synthetic transcript only — no real student/uploaded/production data sent to IFM.
```

## What it must prove
ONE real authenticated call through the configured K2Think provider, for **both** summary jobs as
routed (brief = default route; detailed = `use_nvidia`), both currently `K2-Think-v2`. **Assert the
echoed model ID matches the configured identifier (rule 11)** — for each route.

## Evidence — RECORDED (operator run, 2026-06-11, secrets REDACTED)
Run: `backend/scripts/gate3_smoke.py` via a throwaway container with the rotated key injected from the
operator shell (key never written to a file, log, or the container image).
```
- brief    : model echo = MBZUAI-IFM/K2-Think-v2 ; backend_used=cerebras ; route_source=requested ;
             elapsed 11.9s ; finish_reason=stop ; parseable BriefSummary {"text": ...}   ✅
- detailed : model echo = MBZUAI-IFM/K2-Think-v2 ; backend_used=nvidia   ; route_source=requested ;
             elapsed 39.8s ; finish_reason=stop ; parseable DetailedSummary (all required sections) ✅
- rule-11 model-ID echo matches the configured identifier on BOTH routes.
- response_format (json_object / JSON mode) HONORED by K2-Think-v2 (probe).
- Authorization header + all secrets REDACTED; synthetic transcript only.
- Result: PASS.
```
Triage findings fixed to reach this PASS (real-call-only, never surfaced by the deterministic
provider): F-4.5-48 (brief: inline-reasoning truncation + last-valid-object extraction) and F-4.5-49
(detailed: route-aware request timeout). See [[steps/stage-04/4.5d]].

## Outcome
- [x] Recorded by operator on 2026-06-11. **PASS.**
- Gate 3 green + Gate 1 (full E2E suite) + Gate 2 (forced-fault) ⇒ **Stage 4.5 → FULLY VERIFIED**
  (roadmap stamped in this same commit). Open after: F-4.5-27, F-4.5-28 (provider/access reality).
