---
type: session-report
stage: 04
session: "4.7-P1"
slug: stage3-restore
status: complete
created: 2026-06-11
updated: 2026-06-11
spec: knowledge/specs/stage-04/4.7-student-facing-summaries.md
plan: knowledge/plans/stage-04/4.7a-student-summary-read-policy.md
commit: ""
---

# Stage 4.7 P1 — Stage 3 Content-Visibility E2E Restore — Report

## Linked documents
- Spec: [[specs/stage-04/4.7-student-facing-summaries]]
- Plan: [[plans/stage-04/4.7a-student-summary-read-policy]]
- Source (parent): [[archive/stage-04/4.3.5d/4.3.5d-checkpoint-E-report]]
- Source (revocation): [[archive/stage-04/4.3.5d/4.3.5d-checkpoint-E2-signed-url-revocation]]
- Restored spec file: `tests/e2e/4.7-stage3-content-visibility.spec.ts`

## Result
**PASS — green as-is, NO assertion drift.** The reconstructed spec passed against the CURRENT
auth/session/API contract on the first run. The P1 circuit breaker (rewrite-no-assertions / halt-if-drift)
did **not** trigger — the Stage 3 content-visibility contract is intact.

## What "restore" meant here
There was **no committed Stage 3 `.spec.ts`** to un-archive — the original gate (session 4.3.5d
Checkpoint E + supplemental E2) executed via a throwaway CommonJS runner (`/tmp/checkpoint-e-gate.cjs`,
`/tmp/checkpoint-e2-revocation.cjs`) that was never committed. Per rule 14 ("an archived spec is a dead
spec"), the assertions were **re-authored from the 4.3.5d markdown reports** into a live Playwright spec
now in the active suite: `tests/e2e/4.7-stage3-content-visibility.spec.ts`.

## Reconstruction-assurance: assertion → source-line mapping
Every assertion traces to a line in the 4.3.5d Checkpoint E / E2 reports. Strength: asserted on **server
payloads AND rendered UI**, on **bodies not just status codes**, with **session preserved**.

| # | Restored assertion (spec line) | Source (4.3.5d report line) |
|---|---|---|
| 1 | Admin creates module (product path) + assigns student; 4 sections generated | E report L37, L55 ("fresh module through admin path", sections Lecture 1/2, Lab 1, Assignment 1) |
| 2 | Lecturer uploads valid PDF → 201, processingStatus present | E report L57, L83 ("PDF uploaded", asset processingStatus Completed) |
| 3 | Lecturer replaces by asset id → 200, same id, filename changes | E report L58–L59 ("PDF replaced", "Asset row/id used for replace") |
| 4 | Non-PDF upload → 422, Lecture 2 has no asset | E report L60 ("non-PDF upload rendered role=alert … did not create an asset row"); contract code from `test_content.py:388` (`b"not a pdf"` → 422) |
| 5 | Lecturer notes saved (PATCH) → 200 | E report L56 ("Notes saved … verified after reload") |
| 6 | Publish Lecture 1 → 200 publishStatus=published; others draft | E report L61–L62, L82 ("Published section: Lecture 1", visibility Published) |
| 7 | **Student server response titles EXACTLY ['Lecture 1']; ids exactly [Lecture1]** | E report L71–L73 ("Student response section titles ['Lecture 1']", ids) |
| 8 | Student detail: lecturerNotes == notes; assets include replacement filename | E report L74–L75 ("Lecturer notes visible", "Asset visible: …replacement.pdf") |
| 9 | Draft section direct fetch → 404 SECTION_NOT_FOUND | E report L69–L70 (draft sections absent); current content contract (`get_published_section_for_student` → None → 404) |
| 10 | Signed URL for published asset → 200 with url | E report L76 ("Signed URL HTTP status: 200") |
| 11 | Student upload attempt → 403 CONTENT_FORBIDDEN; `/me` still student | E report L77–L78 ("Student upload rejection 403", "/me still student") |
| 12 | Student UI: only Lecture 1 heading visible; Lecture 2/Lab 1/Assignment 1 absent; notes + filename shown; not /login | E report L68–L70, L74–L75 |
| 13 | Unpublish Lecture 1 → 200 unpublished | E2 report L72–L76 ("Unpublish Lecture 1", visibility Unpublished) |
| 14 | After unpublish, student section list → `[]`; `/me` still student | E2 report L84 ("GET …/sections -> []"), L83/L91 |
| 15 | **Fresh signed-URL after unpublish → 403 CONTENT_FORBIDDEN (NOT 404)** | E2 report L122–L142 (E2-B1 passing rerun: `403 {"detail":"CONTENT_FORBIDDEN"}`) |
| 16 | Student UI reload → Lecture 1 gone | E2 report L90 ("Lecture 1 absent from student UI") |

**Audit note for human review (G8 + security gates):** these assertions are the live regression for
"students never see unpublished content." Assertion 7 uses strict equality (`toEqual(['Lecture 1'])`),
assertion 15 explicitly asserts `403 CONTENT_FORBIDDEN` AND `not 404` (the E2-B1 contract), and the UI
checks use `exact: true` heading matches. Review these for strength, not just green/red.

## Verification
| Command | Result | Notes |
|---|---|---|
| `npx playwright test --list` | passed | 10 tests / 8 files; new spec compiles + listed |
| `node tests/e2e/fixtures/seed.mjs` (E2E_RUN_ID=e2e-1781207168-stage3restore) | passed | 4 auth users, manifest created |
| `npx playwright test 4.7-stage3-content-visibility.spec.ts` | **1 passed (4.9s)** | green as-is, no drift |
| `node tests/e2e/fixtures/teardown.mjs <runId>` | passed | removed 1 module, 4 sections, 2 memberships, 1 asset, 1 storage object; standing users preserved |

## Deviations from the original
- **Lecturer setup (upload/replace/notes/publish) is driven via the real backend API**, not UI clicks.
  Rationale: the load-bearing Stage 3 guarantee is the **student visibility boundary**, which is asserted
  on the real student server payload AND the real student browser UI. Lecturer-side setup through the real
  content endpoints still exercises the real backend/DB/storage (no mocks on the critical path, rule 9);
  it trades a flakier UI-driven upload for a deterministic, equally-real product path. The student-facing
  assertions (the visibility regression) remain browser + API. Recorded as `accepted with rationale` (§17).
- Inline minimal `%PDF-` buffers replace fixture PDFs (the validator only checks magic + non-empty).

## Findings
- **None.** No assertion drift; the contract is current. No §17 finding required.

## Knowledge updates
- New active spec `tests/e2e/4.7-stage3-content-visibility.spec.ts` (now part of the rule-14 suite).
- This report.

## Close-the-loop checklist
- [x] Assertions restored to the ACTIVE Playwright suite (rule 14)
- [x] Re-run GREEN against the current contract
- [x] Assertion → source-line mapping recorded (reconstruction auditability)
- [x] Verification commands run; real output recorded
- [x] Run artifacts torn down (manifest-driven); standing users preserved
- [x] No unresolved findings

## Change history
- 2026-06-11 — initial completion; P1 green as-is, no drift.
