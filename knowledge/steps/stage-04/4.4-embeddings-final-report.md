---
type: final-report
stage: "4.4"
session: "4.4"
slug: embeddings-final-report
status: fully-verified
created: 2026-06-10
updated: 2026-06-10
spec: knowledge/specs/stage-04/4.4-embeddings.md
plan: knowledge/plans/stage-04/4.4-embeddings.md
---

# 4.4 Embeddings Final Report

## Linked documents
- Spec: [[specs/stage-04/4.4-embeddings]]
- Plan: [[plans/stage-04/4.4-embeddings]]
- Regression: [[steps/stage-04/4.3.5e-regression-under-projection]]
- Schema preflight: [[steps/stage-04/4.4-schema-preflight]]
- Truncation assessment: [[steps/stage-04/4.4-truncation-assessment]]
- Embedding worker: [[steps/stage-04/4.4-embedding-worker]]
- Browser gate: [[steps/stage-04/4.4-browser-gate]]
- Report: [[steps/stage-04/4.4-embeddings-final-report]]

## Summary
Stage 4.4 is fully verified.

Implemented:

- C-lite processing-status projection at `GET /modules/{module_id}/sections/{section_id}/transcript-processing-status`.
- `pgvector.sqlalchemy.Vector(384)` transcript chunk embeddings with provenance constraint.
- Embed-only active-job partial unique index.
- Local `sentence-transformers/all-MiniLM-L6-v2` embedding worker with pinned revision `1110a243fdf4706b3f48f1d95db1a4f5529b4d41`.
- Separate embedding worker queue inside the existing transcript ingestion pipeline.
- Per-row canonical embedding input hash using `transcript_chunks.chunking_version` as the semantic `chunkerVersion`.
- Lecturer projection-driven `Embedding` / `Embedded` / `Failed` states.
- 4.4 browser gate and 4.3.5e projection regression.

Not implemented:

- summaries
- K2Think
- AIRequestLog
- LLMProvider
- PromptRegistry
- vector index
- retry UI
- semantic search
- assistant retrieval
- student-facing embedding output
- transcript viewer
- embedding debug viewer

## Final Verification
Docs commit audit:

```text
HEAD=8860a6e docs: consolidate recovery session reports
HEAD contained only 4.3.5 documentation consolidation files.
No backend/frontend/tests/compose/env/4.4 knowledge files were captured in the docs commit.
```

Dirty tree audit:

```text
Remaining dirty files were Stage 4.4 implementation, tests, generated client, compose/env, E2E, and knowledge files.
git diff --check: no output
```

Generated client:

```bash
bash scripts/generate-api-client.sh
git diff --exit-code frontend/src/lib/api
```

Result:

```text
Generator exited 0.
The diff is the intentional uncommitted Stage 4.4 generated-client diff relative to HEAD.
No unexpected generated files appeared.
```

Backend:

```bash
docker compose exec backend pytest
```

Result:

```text
191 passed, 83 warnings in 15.09s
```

Frontend:

```bash
docker compose exec frontend npx tsc --noEmit
docker compose exec frontend npx next build
```

Result:

```text
tsc passed with no output
Next.js build compiled successfully and completed production build
```

Guardrails:

```bash
rg -n "\bfetch\s*\(" frontend/src/app frontend/src/components frontend/src/features frontend/src/lib \
  --glob '!frontend/src/lib/api/core/request.ts' \
  --glob '!frontend/src/lib/api/upload.ts'

rg -n "jwt-decode|decodeJwt|app_metadata|user_metadata|session\.user.*role" frontend/src
```

Result:

```text
no direct fetch violations
no JWT role decoding
```

## Browser Gates
4.4 browser gate:

```text
run id: e2e-1781089037-4-4-final
test: tests/e2e/4.4-embedding-browser.spec.ts
result: 1 passed (19.0s)
teardown: first teardown removed run-owned artifacts; second teardown was idempotent
```

4.3.5e projection regression:

```text
run id: e2e-1781089206-4-3-5e-regression
test: tests/e2e/4.3.5e-transcript.spec.ts
result: 1 passed (12.3s)
teardown: first teardown removed run-owned artifacts; second teardown was idempotent
```

The 4.4 browser gate asserts projection-driven completion:

```text
overallState === "embedded"
steps.embed.status === "completed"
embeddedChunkCount === chunkCount
safeFailureMessage === null
```

The 4.3.5e regression asserts parse/chunk step completion and allows the pipeline to advance to `embedded` when Stage 4.4 is active.

## Embedding DB and Provenance Evidence
Recorded Checkpoint G proof:

```text
embed ingestion job status: completed
chunk_count=1
embedded_count=1
vector dimension=[384]
embedding_model=["sentence-transformers/all-MiniLM-L6-v2"]
embedding_model_revision=["1110a243fdf4706b3f48f1d95db1a4f5529b4d41"]
embedding_dimension=[384]
embedding_normalization=["l2"]
embedding_version=["embedding-v1"]
embedding_input_hash set for every embedded chunk
chunking_version=["chunk-v1-no-overlap-180w"]
active_embed_jobs=0
```

Duplicate delivery and stale-row proof:

```text
embed-only active partial unique index blocks duplicate queued/running embed jobs
stale batch selection uses FOR UPDATE SKIP LOCKED
embedding persistence rechecks current provenance and input hash before row mutation
partial failure resume tests cover stale/missing row behavior
```

## Authz and Student Visibility
Recorded browser/API proof:

```text
student processing-status endpoint: 403 TRANSCRIPT_FORBIDDEN
student session: preserved; no redirect to /login
unassigned lecturer processing-status endpoint: 404 SECTION_NOT_FOUND
student UI: no embedding phase, chunk counts, embeddedChunkCount, vectors, embedding metadata, model metadata, raw chunk text, transcript internals, or stack traces
```

## Truncation Assessment
Recorded in [[steps/stage-04/4.4-truncation-assessment]]:

```text
maxWordPieceCount=78
p95WordPieceCount=66
countAbove256=0
resolution=accepted
```

## Artifact Hygiene
Run manifests:

```text
tests/e2e/.runs/*.json removed after evidence capture
git status --short tests/e2e/.runs: no output
git ls-files --others --exclude-standard tests/e2e/.runs: no output
```

Model/cache artifacts:

```text
No model weights or cache artifacts are tracked or staged.
The broad hygiene grep matches legitimate source paths containing `models/`; no `cache`, `sentence`, `transformers`, `onnx`, `safetensors`, `.bin`, or `.pt` artifacts are present in git status.
```

## Modified Prior Sessions
- Session 4.3.5e — `tests/e2e/4.3.5e-transcript.spec.ts`: changed the regression to assert parse/chunk step completion under the Stage 4.4 projection instead of exact terminal `overallState === "chunked"`.
- Session 4.3.5e — `knowledge/steps/stage-04/4.3.5e-regression-under-projection.md`: recorded the supersession decision and rerun evidence.

## Status
Stage 4.4 Embeddings: FULLY VERIFIED.

Next stage: Stage 4.5 planning for AIRequestLog + summary generation.
