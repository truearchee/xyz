# Stage 4.9c — Restyle migration inventory (§5)

> Produced **LIVE** during 4.9c — one row per surface at the moment it is touched (developer hold #1;
> an inventory written from memory after the fact is fiction). This is also the evidence that
> "componentize-or-leave" (§5) was decided **per surface**, not drifted into a full repaint.
>
> Legend — **Restyled?** yes / partial / no(left). **Hooks preserved?** every data-testid / aria-label /
> getByRole name / role / aria-live byte-for-byte (or spec updated same-commit — noted). **Inline removed?**
> no `style={` left except sanctioned runtime-dynamic values. **Stage 12?** left for the consistency sweep.

| Surface | Route / file | Restyled? | Reason (§5) | Hooks preserved? | Inline removed? | Stage 12 backlog? |
|---|---|---|---|---|---|---|
| Login | `(auth)/login/page.tsx` | yes (light) | §2.0 named; **0 inline styles pre-existing** so no debt to remove — token pass + Input/Button/Card; NOT Stage-5-reused | yes — `getByLabel("Email"/"Password")`, buttons "Sign in"/"Sign out"/"GET /me", session-debug text kept visible | n/a (was 0) | no (deeper polish → Stage 12) |
| Transcript status badge (4.5d/4.6) | `features/content/lecturer/TranscriptStatusBadge.tsx` | yes | named 4.5d/4.6 panel; status-by-text tonal tokens; high reuse | yes — `section-transcript-status/steps/retry-${sectionKey}`, `role=status`/`alert`, `aria-live=polite`; step text ("completed/failed/…") unchanged | yes | no |
| Transcript summary panel (4.5d) | `features/content/lecturer/TranscriptSummaryPanel.tsx` | yes | named 4.5d panel; high reuse | yes — `section-summary-panel/brief/detailed/brief-status/detailed-status-${sectionKey}`, `aria-label="Lecture summaries"`, `role=status`/`alert` | yes | no |

**Slice-1 confirmation:** tsc + build clean; inline-style + token-purity grep clean over the 3 files; full active Playwright suite re-run (the gate) — see the 4.9c report.

### Remaining §5-eligible surfaces — NOT yet touched (slice 2+, this stays honest)
- Lecturer: `LecturerModuleDetail`, `SectionTranscriptControl`, `SectionUploadControl`, `SectionAssetList`, `SectionAssetRow`, `SectionNotesEditor`, `SectionPublishControl`
- Student: `StudentModuleDetail`, `StudentSectionDetail`, `StudentSectionView`, `StudentAssetRow`, `SummaryMarkdown`
- Admin: `admin/page.tsx`, `features/admin/shared.ts` (panelStyles), `users/*`, `modules/*`
- Shared: `features/modules/ModuleListView`
(Each will get its row at the moment it is restyled; one-offs Stage 5+ never reuses will be marked `no(left)` + Stage 12.)
