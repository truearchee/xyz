# Stage 4.9c — Restyle migration inventory (§5)

> Produced **LIVE** during 4.9c — one row per surface at the moment it is touched (developer hold #1;
> an inventory written from memory after the fact is fiction). This is also the evidence that
> "componentize-or-leave" (§5) was decided **per surface**, not drifted into a full repaint.
>
> Legend — **Restyled?** yes / partial / no(left). **Hooks preserved?** every data-testid / aria-label /
> getByRole name / role / aria-live byte-for-byte (or spec updated same-commit — noted). **Inline removed?**
> no `style={` left except sanctioned runtime-dynamic values. **Stage 12?** left for the consistency sweep.

| Surface | Route / file | Restyled? | Reason (§5) | Hooks preserved? | Inline removed? | Stage 12 backlog? |
|---|---|---|---|---|---|---|
| Login | `(auth)/login/page.tsx` | yes (light) | §2.0 named; **0 inline styles pre-existing** so no debt to remove — token pass + Input/Button/Card; NOT Stage-5-reused | yes — `getByLabel("Email"/"Password")`, buttons "Sign in"/"Sign out"/"GET /me", session-debug text kept visible | n/a (was 0) | no (deeper polish → Stage 12) |
| Transcript status badge (4.5d/4.6) | `features/content/lecturer/TranscriptStatusBadge.tsx` | yes | named 4.5d/4.6 panel; status-by-text tonal tokens; high reuse | yes — `section-transcript-status/steps/retry-${sectionKey}`, `role=status`/`alert`, `aria-live=polite`; step text ("completed/failed/…") unchanged | yes | no |
| Transcript summary panel (4.5d) | `features/content/lecturer/TranscriptSummaryPanel.tsx` | yes | named 4.5d panel; high reuse | yes — `section-summary-panel/brief/detailed/brief-status/detailed-status-${sectionKey}`, `aria-label="Lecture summaries"`, `role=status`/`alert` | yes | no |

**Slice-1 confirmation:** tsc + build clean; inline-style + token-purity grep clean over the 3 files; full active Playwright suite re-run (the gate) — see the 4.9c report.

### Slice 2 — lecturer content (all restyled; full-suite gated)
| Surface | Route / file | Restyled? | Reason (§5) | Hooks preserved? | Inline removed? | Stage 12? |
|---|---|---|---|---|---|---|
| Lecturer module detail | `features/content/lecturer/LecturerModuleDetail.tsx` | yes | §2.2; high reuse | yes — `lecturer-section-list`, `lecturer-section-row-${key}`, aria-busy/role=alert, aria-labelledby; active/inactive → Badge | yes | no |
| Section transcript control | `features/content/lecturer/SectionTranscriptControl.tsx` | yes | §2.2 + 4.6d replace/retry; high reuse | yes — `section-transcript-control/pending/replace-control/replace-upload/replace-confirm/replace/upload/status/error-${k}`, aria-label, labels, button names, role=status/alert (buttons kept raw to preserve testids) | yes | no |
| Section upload control | `features/content/lecturer/SectionUploadControl.tsx` | yes | §2.2 reuse | yes — `section-upload-control-${k}`, aria-label, "PDF file", role=alert | yes | no |
| Section asset list | `features/content/lecturer/SectionAssetList.tsx` | yes | §2.2 reuse | yes — aria-label "Assets for …" | yes | no |
| Section asset row | `features/content/lecturer/SectionAssetRow.tsx` | yes | §2.2 reuse | yes — `section-asset-row/processing-status/replace-${id}`, "Replacement PDF", role=alert | yes | no |
| Section notes editor | `features/content/lecturer/SectionNotesEditor.tsx` | yes | §2.2 reuse | yes — `aria-label="Lecturer notes for …"`, aria-live=polite, role=alert | yes | no |
| Section publish control | `features/content/lecturer/SectionPublishControl.tsx` | yes | §2.2 reuse | yes — `section-publish-status-${k}`, `aria-label="${Publish|Unpublish} …"`, role=alert | yes | no |

**Slice-2 no-behavior check:** no design-plan state the thin v1 skipped surfaced in the lecturer area; all existing states (loading/empty/error/forbidden/pending/confirm/upload/replace) carried over 1:1 with the new visual. No finding raised.

### Slice 3 — student content (all restyled; full-suite gated)
| Surface | Route / file | Restyled? | Reason (§5) | Hooks preserved? | Inline removed? | Stage 12? |
|---|---|---|---|---|---|---|
| Student module detail | `features/content/student/StudentModuleDetail.tsx` | yes | §2.3 mobile-first; high reuse | yes — `student-section-list`, `student-section-summary-flag-${id}`, `student-section-open-${id}`, aria-busy/role/labelledby; active/inactive → Badge; summary flags → tonal classes | yes | no |
| Student section detail | `features/content/student/StudentSectionDetail.tsx` | yes | §2.3; high reuse | yes — `student-section-detail`, `student-summary-brief`/`detailed`, `data-state`, aria-busy/label/labelledby, role=alert/status | yes | no |
| Student section view | `features/content/student/StudentSectionView.tsx` | yes | §2.3 reuse | yes — `student-section-row-${k}`, aria-label "Lecturer notes for…/Files for…/Published files for…" | yes | no |
| Student asset row | `features/content/student/StudentAssetRow.tsx` | yes | §2.3 reuse | yes — `student-section-asset-row-${id}`, `aria-label="Open file …"`, role=alert | yes | no |
| Summary markdown | `features/content/student/SummaryMarkdown.tsx` | yes | §2.3; the summary render Stage 5+ reuses | yes — `data-testid` passthrough; sanitization (disallowed elements, SafeLink) UNCHANGED | yes | no |

**Slice-3 no-behavior check:** no skipped design-plan state surfaced; all states (loading/missing/error/generating/unavailable/capped) carried 1:1. No finding.

### Slice 4 — admin + shared module listing (full-suite gated)
| Surface | Route / file | Restyled? | Reason (§5) | Hooks preserved? | Inline removed? | Stage 12? |
|---|---|---|---|---|---|---|
| Admin shell page | `app/(app)/admin/page.tsx` | yes | §2.1 | yes — "Admin" heading | yes | no |
| Admin shared styles | `features/admin/shared.ts` | yes | shared dep consumed by all admin panels | n/a (no markup) — `panelStyles` (CSS) → `panelClasses` (className strings); **renamed so the compiler flags any dangling importer (hold #1)** | yes | no |
| Admin users panel | `features/admin/users/AdminUsersPanel.tsx` | yes | §2.1; validates Table/Card patterns Stage 5+ reuses | yes — `admin-users-table`, `admin-user-row-${slug}`, aria-label, role=alert, aria-busy | yes | no |
| Create user form | `features/admin/users/CreateUserForm.tsx` | yes | §2.1 | yes — `create-${role}-form`, `aria-label="${role} email/full name/password/timezone"`, role=alert | yes | no |
| Reset password form | `features/admin/users/ResetPasswordForm.tsx` | yes | §2.1 | yes — `aria-label="New password for …"`, role=alert/status, aria-live | yes | no |
| Admin modules panel | `features/admin/modules/AdminModulesPanel.tsx` | yes | §2.1 | yes — `admin-modules-table`, `admin-module-row-${slug}`, `aria-label="Managed module"/"Module members"`, **`getByRole('heading',{name:"${title} members"})`**, aria-busy, role=alert | yes | no |
| Create module form | `features/admin/modules/CreateModuleForm.tsx` | yes | §2.1 | yes — `create-module-form`, `aria-label="Module title/owner lecturer/description/timezone/starts on/ends on"`, role=alert | yes | no |
| Assign member form | `features/admin/modules/AssignMemberForm.tsx` | yes | §2.1 | yes — `assign-member-form`, `aria-label="Assignment module/role/user"`, role=alert | yes | no |
| Module list view | `features/modules/ModuleListView.tsx` | yes | shared role-based listing (admin/lecturer/student homes) | yes — aria-label "Modules", aria-current="page", role=alert; active/inactive → Badge | yes | no |
| **Module detail view** | `features/modules/ModuleDetailView.tsx` | **no (LEFT)** | **§5: unconsumed — only re-exported by `features/modules/index.ts`, no JSX renders it, no E2E hooks.** Restyling dead code violates YAGNI/§5. | n/a | no (kept inline) | **yes — Stage 12 (restyle or delete)** |

**Slice-4 notes:** `panelStyles` removed with **zero dangling importers** (tsc clean + grep = comment-only; hold #1). Inline-style grep over the *restyled* admin/modules surfaces is clean; the only residual `style={}` in `features/modules/` is `ModuleDetailView` (deliberately LEFT). No no-behavior finding (admin form fields kept their `aria-label`s distinct from visible labels, so `getByLabel` is byte-preserved; buttons were previously unstyled → token Button classes added, no state change).

---

## 4.9c completion summary
- **§5-eligible surfaces restyled:** login + all lecturer content + all student content + all admin + ModuleListView (24 surfaces across 4 full-suite-gated slices).
- **Left for Stage 12 (§5 componentize-or-leave):** `ModuleDetailView` (unconsumed). This is the Stage-12 Part-3 backlog from 4.9.
- **Inline styles removed** from every restyled surface; **token-purity** clean (no `var(--palette)`/hex in restyled code).
- **Every E2E hook preserved byte-for-byte** — `git diff tests/` EMPTY across all 4 slices (no spec touched, no selector loosened).
- **No behavior added** — no design-plan-state-not-implemented finding arose in any slice.
- **Full active suite GREEN (11/11)** after each slice; final suite green at slice 4.
