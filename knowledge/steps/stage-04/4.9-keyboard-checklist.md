# Stage 4.9 — Manual keyboard accessibility checklist (§6.4 / §12 proof obligation 8)

> §6.4: dynamic keyboard behaviour (focus trap / Esc / focus-return / skip-link / tab order) is a
> **manual pass**, NOT automated here — full automated keyboard + axe is **Stage 12** (ADR-048). This
> artifact records each item, its **expected** behaviour, the **implementation basis** (what in the code
> guarantees it), and a **status**. The truly-dynamic browser keypress checks are owned by the developer
> (I cannot drive interactive keyboard input headlessly, and automated keyboard tests are Stage-12 scope).

Run against: the restyled surfaces on the dev stack (`http://localhost:3000`, e2e overlay). Tester: ____ · Date: ____

| # | Item | Expected | Implementation basis (code-confirmed) | Status |
|---|---|---|---|---|
| 1 | **Skip-to-content link** | Tab once on any `(app)` page → "Skip to content" appears (focus-visible); Enter moves focus to `<main>` | `AppShell` renders `<a href="#main-content" class="sr-only focus:not-sr-only …">` as the first focusable; `<main id="main-content">` exists | ☐ pending dev keypress |
| 2 | **Visible focus ring** everywhere | Every interactive element shows the violet focus ring on keyboard focus | `focusRing` token classes (`focus-visible:ring-2 ring-focus-ring ring-offset-2`) on Button/Input/InteractiveCard/SortableHeader/nav/links/raw buttons | ☐ pending dev keypress |
| 3 | **Modal focus trap** | Open a Modal → Tab cycles within the dialog, never escaping to the page behind | `Modal` built on React Aria `ModalOverlay`/`Modal`/`Dialog` — focus containment by construction | ☐ pending dev keypress |
| 4 | **Modal Esc closes** | Esc closes the Modal | React Aria `ModalOverlay isDismissable` — Esc handling by construction | ☐ pending dev keypress |
| 5 | **Modal focus return** | On close, focus returns to the trigger | React Aria restores focus to the trigger by construction | ☐ pending dev keypress |
| 6 | **Toast keyboard-dismiss** | The toast dismiss (✕) is reachable + actionable by keyboard; error toasts persist until dismissed | `Toast` dismiss is a real `<button aria-label="Dismiss notification">` with focus ring; errors don't auto-dismiss (asserted in the §6.4 smoke) | ☐ pending dev keypress |
| 7 | **Tab order is logical** | Tab order follows visual/reading order on login, admin, lecturer detail, student detail | Restyle preserved DOM order (className-only changes); no `tabindex` reordering introduced | ☐ pending dev keypress |
| 8 | **Forms operable by keyboard** | Labels focus their inputs; errors are announced (aria-describedby / role=alert) | label/`aria-label` associations + `aria-describedby`/`role=alert` preserved (asserted in the §6.4 Input smoke + the restyle inventory) | ☐ pending dev keypress |
| 9 | **Reduced motion** | With OS "reduce motion", the pipeline shimmer + transitions are removed | `@media (prefers-reduced-motion: reduce)` global guard in globals.css; `motion-safe:` on the active step | ☐ pending dev keypress |

**What IS already verified automatically (so the manual pass is confirmation, not discovery):**
- The §6.4 static-a11y smoke (vitest) asserts: Button real `<button>` + aria-busy; Input label assoc + aria-describedby; Badge text label; Modal `role=dialog` + labelled + initial focus target; Table semantic + button sort headers; Toast live region + error-no-auto-dismiss; Progress failed = explicit text.
- The behavioural primitives for items 3–5 come from React Aria (a library whose focus management is itself tested) — the manual pass confirms our wiring, not the library.

**Sign-off:** dynamic keyboard pass completed by ____ on ____ → all items ☑. (Until then: implementation-confirmed; awaiting the human keypress pass — owner: developer. Full automated keyboard + axe audit: Stage 12, ADR-048.)
