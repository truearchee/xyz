---
type: verification-report
stage: 11
session: "11.6"
slug: real-provider-smoke
status: pass
created: 2026-06-21
updated: 2026-06-21
---

# Session 11.6 — Real-Provider Smoke

## Result
PASS — the Stage 11.6 grade-forecast advice route made real, non-streaming K2Think provider calls and,
for both a reachable (`requires_high_score`) and an `impossible` synthetic forecast, the response model
echo matched the configured prompt model and the production schema + numeric/contradiction +
student-copy-safety validators passed (rule 11).

The key was read from `.env` and never printed; inputs are SYNTHETIC `ForecastResult` fixtures (no real
student or course data sent to IFM). Script: `backend/scripts/gate11_advice_smoke.py`.

## Evidence (key redacted)

| Field | reachable case | impossible case |
|---|---|---|
| Provider | `k2think` | `k2think` |
| Endpoint | `https://api.k2think.ai/v1/chat/completions` | same |
| Route/backend | `cerebras` | `cerebras` |
| Prompt | `grade_forecast_advice/v1` | `grade_forecast_advice/v1` |
| Configured model | `MBZUAI-IFM/K2-Think-v2` | `MBZUAI-IFM/K2-Think-v2` |
| Echoed model | `MBZUAI-IFM/K2-Think-v2` | `MBZUAI-IFM/K2-Think-v2` |
| Echo assertion | pass | pass |
| Status code | `200` | `200` |
| Finish reason | `stop` | `stop` |
| Attempts | `1/3` | `1/3` |
| Latency | `30.5s` | `33.5s` |
| Usage | `prompt=537 completion=2199 total=2736` | `prompt=547 completion=2563 total=3110` |
| Parsed schema | `GradeForecastAdvice` (125 chars) | `GradeForecastAdvice` (223 chars) |
| Validators | numeric consistency + contradiction + student-copy safety passed | same |

## Notes
- Masked env resolution at run time: `provider=k2think`, `base_url=https://api.k2think.ai`,
  `LLM_API_KEY=present length=20`, `prompt_model=MBZUAI-IFM/K2-Think-v2`. `.env.e2e` carries no `LLM_`
  override; `backend/.env` is absent.
- K2-Think-v2 reasons inline in `content` with no request-level reasoning control (provider note
  F-4.5-04: `reasoning_level` is never faked). The prompt is anchored on rewriting the deterministic
  `templateAdvice` to keep the reasoning trace short; `max_tokens=4000` keeps the completion under the
  60s route timeout. The smoke retries up to 3× per case (production retries twice then falls back to
  the deterministic template) — both cases passed on attempt 1.
- Rule 11 is satisfied: the route returned the CONFIGURED model id (not a silent deployment alias) and
  the real model output passed every production validator for both the number-citing reachable case and
  the sensitive impossible case.
