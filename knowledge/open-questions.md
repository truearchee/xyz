# Open questions

Decision inbox. Capture unresolved questions here cheaply. Promote to an ADR in
`decisions/` only when the decision is durable; then mark the question resolved with a
link to the ADR.

Format: `- [ ] <question> — raised YYYY-MM-DD (#stage.session)`

---

- [x] Queue tech for Stage 4 worker sessions resolved to RQ on Redis per locked roadmap guidance and existing Stage 1 worker service — resolved 2026-06-01 by [[decisions/adr-017-ingestion-job-worker-spine]] (#4.2)
- [ ] Replace the temporary MVP default module-section generation policy with schedule-driven or template-driven generation, or explicitly accept the default as longer-lived — raised 2026-06-07 (#4.3.5d-B1)
- [x] Confirm the `summarizing` overallState added to the transcript projection (intermediate between `embedded` and `summarized`, for the passive "generating" badge §13), or fold it back into `embedded` — kept; 4.5b rests at `summarizing` when brief is done and detailed is deferred (`ENABLE_DETAILED_SUMMARY=false`) — resolved 2026-06-11 (#4.5b)
- [ ] Gate 2.A authority-hash residual: the Stage 4.5 spec embeds no reference roadmap hash, so the check is presence + v3 + obligation-consistency; the recorded SHA-256 is the baseline of record for future drift detection — raised 2026-06-10 (#4.5a)
- [ ] No cross-cutting "rules 1–15" document exists in-repo; the Stage 4.5 spec/roadmap cite rule numbers whose authoritative text lives only in `knowledge/roadmap.md` §"Cross-cutting rules" — confirm that is the canonical source — raised 2026-06-10 (#4.5a)
- [x] `rate_limited` is treated as a terminal `failed` (failure_category=rate_limited) in 4.5a; in-call backoff under the limiter and product re-drive are deferred — confirm the 4.5b/4.6 home — 4.5b adds in-call bounded backoff (limiter-full + provider 429) recorded in-row, terminal on exhaustion; product re-drive stays 4.6 (F-4.5-31) — resolved 2026-06-11 (#4.5b)
- [x] `AIRequestLog.ingestion_job_id` is NOT NULL in 4.5a (all callers are job-tied summaries); Stage 8 assistant gateway calls will need it nullable (migration) — **RESOLVED 2026-06-16 (#5b)**: migration 0020 made it nullable (general "AI calls aren't always ingestion jobs" decoupling — quiz + Stage 8); summary features still require it at the gateway app layer. See [[decisions/adr-045-airequestlog-decoupled-gateway-generalized]]. Stage 8 inherits the nullable column.
- [x] Per-request fault injection (global `LLM_FAULT_INJECTION` can't express inject→clear→succeed) — **RESOLVED 2026-06-16 (#5b)**: `set_request_faults`/`clear_request_faults` in `provider.py`, non-prod-gated (refuses outside `IS_NON_PROD`), popped once per `send()` before the constructor fault; `invalid_input` excluded (pre-transport). Used by the S6 recovery tests. (was #4.5d follow-up)
- [x] **5d quiz generation truncation (`finish_reason='length'`) — PROMOTED to [[findings-5d]] F-5d-1 and RESOLVED 2026-06-16 (#5d).** Fix: `post_class_quiz_generation/v1` `max_tokens` 8000→16000 (CHECKSUMS updated, drift guard green). Real-provider re-confirm at 16000 passed: `finish_reason='stop'`, completion 11350 < 16000, parseable 10-question quiz, rule-11 model echo OK. See [[steps/stage-05/5d-real-provider-smoke]].
- [ ] **Stage 7 glossary definition truncation follow-up.** Real-provider glossary smoke passed rule 11 (`MBZUAI-IFM/K2-Think-v2` model echo matched, parseable definition, Arabic script present), but returned `finish_reason='length'`; raise `backend/prompts/glossary_definition/v1.yaml` `max_tokens` and re-run the smoke so definitions do not truncate mid-sentence — raised 2026-06-17 (#7-core)
- [ ] **Stage 7 detail-panel design follow-up.** The current glossary entry detail UI is an inline aside rather than the tabbed detail sheet from §2.5; defer to the Stage 12 design pass rather than fixing in the 7e reliability follow-up — raised 2026-06-18 (#7e)
- [ ] **Stage 7 knowledge-trio cleanup.** Historical 7a/7bc work did not produce the required per-sub-session spec/plan/report trios under `knowledge/{specs,plans,steps}/stage-07`; clean up as docs maintenance, not as part of the 7e code fixes — raised 2026-06-18 (#7e)
- [ ] **Stage 7 Playwright assertion follow-up.** Add browser-gate assertions for the `<4 terms` multiple-choice fallback and a deterministic MCQ wrong-pick rejection; low-priority and can fold into the 7d gate run — raised 2026-06-18 (#7e)
- [ ] **Stage 7 glossary stuck-row recovery scope.** The Stage 4.6 stuck-row reaper does not cover `glossary_definition_cache`; 7e adds enqueue compensation plus duplicate-save retry recovery, but a later recovery pass should decide whether stale glossary definition cache rows also need scheduled reaper coverage — raised 2026-06-18 (#7e)
- [x] **5d — live browser gate (Gate 1) RUN GREEN 2026-06-16 (#5d-GATE).** Operator authorized the standup (tore down sibling `stage-55`, supplied `/Desktop/LMS/test2/.env`+`.env.e2e`); the `kyiv` stack ran `tests/e2e/5d-post-class-quiz.spec.ts --workers=1` → 1 passed in real Chromium (full path incl. S7 unpublish-mid-attempt 404 + zero events). **Stage 5 FULLY VERIFIED** (Gate 1 + Gate 3 both green). See [[steps/stage-05/5d-student-ui-browser-gate]].
- [ ] **5b residual — AIRequestLog finalize on a mid-CALL crash.** The reaper finalizes the orphaned `running` AIRequestLog via `attempt.ai_request_log_id`, which 5b stamps right after the gateway returns (covers the common crash window: between the AI call returning and the question write). A crash DURING the HTTP call (before `complete()` returns) leaves a `running` log with NO attempt linkage — the SAME pre-existing behavior as summaries — so it is not finalized. Fully closing it needs an AIRequestLog→source reference (the broader Stage-8 AIRequestLog decoupling). — raised 2026-06-16 (#5b)
- [ ] **Stage 4.6 entry requirements** surfaced by the 4.5d close-out: (a) the AI worker runs `worker.work()` without `with_scheduler=True`, so the bounded RQ retry for `invalid_output` is scheduled but never re-drives — 4.6 owns the lecturer retry action + queued/stuck-row sweeper (F-4.5-47); (b) per-request fault injection (replacing global `LLM_FAULT_INJECTION`, which can't express inject→clear→succeed in one run) — raised 2026-06-11 (#4.5d)
- [ ] No static type checker (mypy/pyright) is configured; the 4.5b type-check gate is `compileall` + full pytest collection. Introduce a real type-check pass in Stage 4.9 hygiene — raised 2026-06-11 (#4.5b, spec §15/§16)
- [x] `steps.summary_detailed` uses the established no-job sentinel `not_started`; a distinct `pending` value (passive "generating" affordance) is a 4.5d UI/contract decision — **resolved: keep `not_started`** as the data sentinel; the lecturer UI maps it to a friendly label (no enum/OpenAPI contract change) — resolved 2026-06-11 (#4.5d, F-4.5-41)
- [ ] `AIRequestLog.backend_used` is the REQUESTED route, not response-verifiable — the provider does not echo the served backend; `backend_route_source='requested'` records this, flips to `provider_echoed` if/when the provider ever echoes the route (per-backend budget rests on this assumption at single-model scale) — raised 2026-06-11 (#4.5b, F-4.5-28); see [[decisions/adr-025-routing-split]]
- [ ] Intended models `K2-V2-Instruct` (brief) + `K2-Think-v0` (detailed) are inaccessible; 4.5b/4.5c run BOTH summaries on the one verified `K2-Think-v2` (brief=default route, detailed=use_nvidia route — Option A nominal route separation) — switch back on access (config + prompt-version bump, no Python change) and run the deferred dual-model gate-2.B — raised 2026-06-11 (#4.5b/#4.5c, F-4.5-27/F-4.5-39); see [[decisions/adr-025-routing-split]]
- [ ] Dev/hosted DB drift window: migration `0010` (lifecycleState cutover, drops `is_active`) is applied to `xyz_lms_test` only; the dev DB `xyz_lms` is intentionally left at `0009` because the running pre-4.6a containers expect `is_active`. `0010` must be applied as part of the next backend rebuild+deploy (not before) — raised 2026-06-11 (#4.6a)
- [ ] parse/chunk one-active "current job" partial-unique indexes deferred from 4.6a to 4.6b: a one-active-chunk index breaks the tested two-chunk-jobs-coexist replacement path (`test_same_transcript_different_chunk_keys_serialize_replacement`); 4.6b designs the fencing pointer with the retry that consumes it — raised 2026-06-11 (#4.6a, spec amendment)
- [ ] §12 product-owner policy: supersession RETAINS every replaced transcript + raw file indefinitely (source of truth/retry/audit). Indefinite retention of identifiable recordings is a policy decision, not an engineering default — confirm acceptable for staging/demo or schedule an earlier retention pass — raised 2026-06-11 (#4.6a)
- [ ] Retry endpoint path deviates from the spec's literal `/transcripts/{transcriptId}/retry`: built section-scoped as `POST /modules/{m}/sections/{s}/transcript/{transcriptId}/retry` to reuse the assigned-lecturer section authz and still target a failed pending replacement — confirm acceptable or rename when the 4.6d UI lands — raised 2026-06-11 (#4.6b); see [[decisions/adr-031-retry-resume-from-failed-step-fenced]]
- [ ] `unsupported_file` failure_category has no parse-job producer (file type is rejected at upload with 422 before a parse job exists); parse emits `parse_failed`/`storage_missing`. Category stays defined/mapped — confirm a producer is wanted (e.g. map invalid-VTT-content to it) or leave upload-time-only — raised 2026-06-11 (#4.6b)
- [ ] `crashed` failure_category is mapped in the status projection but only PRODUCED by the 4.6c stuck-row reaper — wire the producer in 4.6c — raised 2026-06-11 (#4.6b)
- [ ] Reaper liveness uses RQ registry (embed/summary stable job_ids) + per-step age (parse/chunk have no stable RQ job_id) — NO heartbeat columns (spec §6 allows it). If parse/chunk false-positives ever appear, add a heartbeat or a stable parse/chunk job_id — raised 2026-06-11 (#4.6c); see [[decisions/adr-032-stuck-row-reaper-singleton]]
- [ ] Recovery triggers (4.6c): the **stuck-row reaper runs automatically at every worker startup** (`REAPER_RUN_AT_STARTUP` default on, singleton-locked). **Storage reconciliation has NO automatic trigger** — admin-trigger-only by default (`RECONCILE_AT_STARTUP` default OFF, to keep worker boot off the recursive storage-listing path), so reconciliation is the recovery job still waiting on a scheduler. **11.1** points a cron at `run_storage_reconciliation`; **4.8** env-hygiene should note `RECONCILE_AT_STARTUP`/`RECONCILIATION_CLEANUP_ENABLED` stay OFF in prod until then — raised 2026-06-11 (#4.6c)
- [ ] Supabase storage `.list()` is non-recursive; `list_objects` recurses + paginates capped at RECONCILIATION_MAX_OBJECTS. A hit cap skips missing-ref detection (capped=True) to avoid false data-loss alarms — revisit if bucket object counts approach the cap — raised 2026-06-11 (#4.6c)
- [ ] Stage 4.6 LIVE browser gate is NOT yet run — 4.6d built the UI + preview endpoint + Playwright specs and verified by pytest/tsc/`--list`, but the live run (rebuild images, apply 0010→0012, restart, `npx playwright`) is the developer's human-watched acceptance. Rule-14 full-suite-green may surface more cross-stage e2e timing fixes (4.6b made summaries run parallel to embed). Until run, Stage 4.6 is "built + verified, browser gate pending" — raised 2026-06-11 (#4.6d)
- [ ] 4.6d retry-flow e2e clears the forced fault by recreating the `embedding_worker` mid-run (`execSync docker compose -f docker-compose.yml -f docker-compose.fault.yml up -d --force-recreate embedding_worker`); depends on docker access from the Playwright runner. If unavailable, fall back to a two-invocation driver (fault run → clear → retry run) like 4.5d — raised 2026-06-11 (#4.6d)
- [x] **RESOLVED F-4.6c-1** (fixed Task 4.6c-F1; live gate green): the 4.6c startup-recovery hook ran `asyncio.run(run_stuck_row_reaper())` on the MODULE engine in the parent before `worker.work()`; RQ forks a job child per task that inherited the dirty pool → `got Future attached to a different loop`. Fix: startup recovery runs on an isolated NullPool engine (`worker.py`), regression test `tests/test_worker_startup_recovery.py`. See [[findings-4.6-gate]] — raised 2026-06-11 (#4.6d-G1), fixed 2026-06-11 (#4.6c-F1)
- [x] **RESOLVED F-4.6b-2** (fixed Task 4.6b-F2; continuity gate green): a fully-processed PENDING replacement never activated because activation was triggered only after summary completion, but the 4.6b DAG decouple let embed finish last with no trigger. Fix: every leaf (embed + brief + detailed) calls idempotent `attempt_pending_activation` (`activation.py`/`embedding_service.py`/`summary_service.py`); 3 ordering regression tests. See [[findings-4.6-gate]] — raised 2026-06-11 (#4.6c-F1), fixed 2026-06-11 (#4.6b-F2)
- [x] **RESOLVED F-4.6d-3** (fixed Task 4.6d-P1; full active suite 9/9 green, retry flow reload-free): C-lite read-contract violation (4.4) in the post-retry status path. Fix landed in the doneness AUTHORITY, not the badge: `_overall_state` + `_failed_step` (`transcript_status.py`) now derive purely from step/job states and no longer short-circuit on the `transcript.status` breadcrumb, so a retried step reports in-progress (not the stale `failed`). 3 regression tests pin both directions; e2e `reload()` workaround removed. See [[findings-4.6-gate]] — raised 2026-06-11 (#4.6b-F2), re-framed (#4.6-CLOSE), fixed 2026-06-11 (#4.6d-P1)
- [ ] **4.7-R2 (non-blocking, accept-with-rationale):** the full active Playwright suite must run `--workers=1` (serial). The pipeline specs (4.3.5e/4.4/4.5d/4.7) under Playwright's default parallelism saturate the single `embedding_worker`; an embed transiently fails (`embedding_failed`) and `waitForTranscriptEmbedded` fails fast on the momentary `transcript.status='failed'` (90s wait < the embed's RQ retry interval `[30,120,300]`s). The product SELF-HEALS (embed RQ-retries to `completed`; all transcripts end completed) — it is CAPACITY, NOT a terminal-failure/concurrency-logic GAP (embed path is FOR UPDATE SKIP LOCKED fenced). Serial is the correct harness config. **REVISIT TRIGGER:** re-examine at the FIRST real parallel-embedding load (Slice 5 / Stage 11 fan-out, or a multi-`embedding_worker` deploy) — the classification is **CAPACITY-at-current-concurrency**, and the exact transient exception class was NOT captured (prior-worker logs rotated); a real concurrency gap, if any, would surface there. Verified ON MAIN that serial is green (full suite 11/11). — raised 2026-06-12 (#4.7-R2); evidence in [[steps/stage-04/4.7b-student-page-browser-gate]]
- [ ] **4.9-hygiene follow-up (from 4.7-R2):** make `tests/e2e/fixtures/db.mjs::waitForTranscriptEmbedded` tolerate a transient `transcript.status='failed'` WHILE embed RQ retries remain (poll through the `[30,120,300]`s retry window instead of failing fast), and/or scale `embedding_worker` in the E2E stack — either would let the active suite drop the `--workers=1` requirement and run in parallel again. — raised 2026-06-12 (#4.7-R2 → 4.9)
- [ ] **Cleanup-when-convenient:** 6 leftover Stage 4.4 E2E modules from a 2026-06-10 session remain in dev `xyz_lms` (titles `Transcript Gate e2e-stage44g-*` / `Embedding Gate e2e-*` / `Stage 4.4 Gate e2e-*`). Not deleted by the 4.7 run (not its artifacts; sacred-rule — don't delete what you didn't create). Remove via their run manifests or a guarded delete when convenient. — raised 2026-06-12 (#4.7-CLOSE)
- [x] **RESOLVED 2026-06-17 (#5.5g): migration-number collision.** Stage 5 is now merged to main and owns `0014` through `0020`. Stage 5.5 was rebased on `origin/main`; its schedule migration is now `0021` and its asset-kind migration is now `0022`. `alembic heads` reports one head, `0022 (head)`. — raised 2026-06-16 (#5a), resolved 2026-06-17 (#5.5g)
- [~] **5.5b suite rework — OBSERVED + fixes APPLIED; runtime green PENDING a free environment.** Neo's attributable run (stack@76f496f, `--workers=1`, run-id matched seed manifest) produced **10 red / 1 pass — all 10 mapped to the prediction, no 9th cause, 4.3.5b's pass confirmed, quarantine = 0 shown.** Two root causes, both fixed via Path B (apply-then-collection-check, runtime green deferred): (1) **section selection by hardcoded title** (4.4, 4.5d-browser, 4.5d-fault, 4.6d, 4.7-stage3, 4.7-student) → `nthSectionOfType(sections, type, ordinal)` swap (filter-by-type; `getSectionsForModule` orders by order_index; throws on miss → also fixes 4.5d-fault's `undefined.title` TypeError); 4.7-stage3 title assertions rewritten to the selected sections' actual titles. (2) **4.3.5c** create-module UI helper didn't fill the new required course-date inputs → filled them. (3) **4.3.5e** generated 64 sections (wide default) + needed an assignment section → pinned a minimal 1-week schedule (deterministic 1 lecture + 1 lab), rewrote the incidental exact-template assertion (generation-shape owned by 5.5a's backend oracle), added `db.mjs insertSection` (non-colliding order_index) and direct-seeded the load-bearing `assignment` section for the unsupported-type guard. **Quarantine: 0** — no spec needed the resolver, so no `fixme` tags exist (the close-out grep gate is therefore trivially satisfied). **`playwright --list` collects 11/9 clean; logic reviewed (no `sectionByTitle` survives; publish-of-assignment confirmed safe by the original spec's prior green).** REMAINING: a runtime green run once a workspace owns the ports (stage-55/kyiv/stage-5 contention — stop the others first; see [[steps/e2e-run-procedure]]). — raised 2026-06-16 (#5.5a), directive (#5.5a-REVIEW), applied (#5.5b-e2e)
- [x] **RESOLVED 2026-06-17 (#5.5g): 5.5a migration seam.** Stage 5.5 schedule config was renamed/revised to `0021` with `down_revision = "0020"`; Stage 5.5 asset kind was renamed/revised to `0022` with `down_revision = "0021"`. No merge-revision file was added. `alembic heads` reports one head. — raised 2026-06-16 (#5.5a), resolved 2026-06-17 (#5.5g)
- [ ] **Admin module-list pagination — tracked debt (deeper issue behind the 7e admin-ordering bugfix).** The 7e fix changed `GET /admin/modules` to newest-first (`created_at DESC, id DESC`) so a just-created module appears on page 1, but the list still returns only the first page (default `limit=50`) and `AdminModulesPanel` has no pagination control — an admin still cannot page past the first 50 to see ALL modules. **Mechanism:** wire the Stage 5 pagination envelope (the intended mechanism) into the admin list endpoint + UI; do NOT build it as part of the ordering bugfix. **Owner:** developer. **Trigger:** Stage 12 hardening, OR before any deployment where module count can exceed one page — whichever comes first. — raised 2026-06-18 (#7e-adminfix)
- [ ] **Sibling list shares the same ASC + fixed-default-limit, no-pagination-UI pattern — flagged not fixed.** `service.list_users` (`backend/app/domains/admin/service.py:372`) still orders `AppUser.created_at, AppUser.id` ASC with router default `limit=50` and no pagination control in `AdminUsersPanel` — the same latent class as the module list before the 7e ordering fix (newly created users fall off page 1 once >50 accumulate). Left unfixed deliberately; fold into the admin-pagination-envelope follow-up above. (`list_module_members` is bounded per-module with no limit and is NOT affected.) — raised 2026-06-18 (#7e-adminfix)
- [ ] **E2E teardown/reset hardening — interrupted runs leave residue.** Manifest teardown only runs on a clean finish, so interrupted Playwright runs strand rows: dev `xyz_lms` had accumulated **86 `course_modules`** (mostly orphans from interrupted E2E runs), which is what surfaced the admin-list ordering bug (new modules fell off page 1). The ordering fix makes the suite green regardless, so this was NOT cleaned to pass. Harden the harness so interrupted runs don't strand rows (e.g. a pre-run reset/sweep of E2E-prefixed rows, or a trap-based teardown). Related leftover-residue note: the 4.4 modules at the cleanup-when-convenient line above. — raised 2026-06-18 (#7e-adminfix)
