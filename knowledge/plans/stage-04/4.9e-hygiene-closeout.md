---
type: session-plan
stage: 04
session: "4.9e"
slug: hygiene-closeout
status: executing
created: 2026-06-13
updated: 2026-06-13
spec: knowledge/specs/stage-04/4.9e-hygiene-closeout.md
report: knowledge/steps/stage-04/4.9e-hygiene-closeout.md
---

# Session 4.9e — Implementation Plan — Hygiene batch + close-out

## Linked documents
- Spec: [[specs/stage-04/4.9e-hygiene-closeout]] · Report: [[steps/stage-04/4.9e-hygiene-closeout]] · Umbrella: [[specs/stage-04/4.9-frontend-foundation-platform-hygiene]]

## Approach
- **Hygiene (backend):** conftest `app=` → `ASGITransport(app=app)` (behaviour-identical); main.py drop
  `allow_credentials` (pure Bearer). Both baked → one image rebuild, then pytest (0 httpx warnings + same
  assertions) + the full E2E suite (CORS-off cross-origin still green).
- **Hygiene (tooling):** `gen:api` alias + README; §7.4 = `check-orphans.mjs` (pre-run fail-loud, DEMONSTRATED
  on a seeded orphan) + `run-active-suite.sh` (`trap cleanup EXIT` = unconditional/finally teardown that also
  supports the 3-part orchestration). Excludes the seed's standing `e2e_module` fixture from the orphan count.
- **Close-out:** `check-contrast.mjs` (re-validate 35 incl. `--color-overlay` composited as a scrim →
  deepened 45%→55% to clear 3:1); `design-system.md` from shipped code (+ large/UI-only solid-status rule);
  design-match screenshots + mobile sanity; ADR-049; resolve F-4.9-1..-5; roadmap.
- **Roadmap honesty (developer's option b):** flip to FULLY VERIFIED **with the human-owned residuals stated
  in the entry** — design-match review (screenshots captured), keyboard pass (checklist ready), remote-CI
  enforcement (F-4.9-5). A bare FULLY VERIFIED hiding pending human passes is not defensible; the entry names them.

## Risks & mitigations
- pytest/E2E share a DB? No — pytest = xyz_lms_test, E2E = xyz_lms (verified). Run both safely.
- CORS-off breaking cross-origin → the full suite (real browser→FastAPI, D1) is the proof.
- globalTeardown vs the 3-part orchestration → resolved via the trap run-script (not globalTeardown).
- A bare "FULLY VERIFIED" hiding human sign-offs → state design-match/keyboard/remote-CI as owned residuals in the roadmap.

## Open questions
- The human design-match sign-off + keyboard pass remain the developer's (I capture evidence + write the checklist);
  the final FULLY VERIFIED human stamp is theirs, consistent with how 4.6/4.7 were human-stamped.
