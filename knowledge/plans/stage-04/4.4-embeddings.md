---
type: session-plan
stage: "4.4"
session: "4.4"
slug: embeddings
status: approved
created: 2026-06-09
updated: 2026-06-10
spec: knowledge/specs/stage-04/4.4-embeddings.md
report: knowledge/steps/stage-04/4.4-embedding-worker.md
---

# Session 4.4 — Implementation Plan — Embeddings

## Linked documents
- Spec: [[specs/stage-04/4.4-embeddings]]
- Plan: [[plans/stage-04/4.4-embeddings]]
- Regression: [[steps/stage-04/4.3.5e-regression-under-projection]]
- Schema preflight: [[steps/stage-04/4.4-schema-preflight]]
- Truncation assessment: [[steps/stage-04/4.4-truncation-assessment]]
- Embedding worker: [[steps/stage-04/4.4-embedding-worker]]
- Browser gate: [[steps/stage-04/4.4-browser-gate]]

## Scope confirmation
Implement Stage 4.4 using the attached spec and plan.

Use subagents only as described in the plan. The main agent owns implementation and final decisions. Subagents are read-only unless explicitly reassigned.

Start with Checkpoint A and Checkpoint B only:
1. Persist the spec and plan.
2. Spawn the read-only subagents:
   - repo_mapper
   - test_mapper
   - migration_reviewer
   - worker_reviewer
   - frontend_contract_reviewer
   - scope_security_reviewer
3. Wait for all subagent results.
4. Consolidate findings.
5. Stop and report confirmed assumptions, deviations, blockers, and the exact next implementation checkpoint.

Do not edit source code beyond persisting the spec/plan until the discovery pass is complete and any spec-vs-repo conflicts are resolved.

Do not begin embedding worker implementation until:
- §3.1 C-lite approval is recorded,
- 4.3.5e regression under projection passes,
- schema preflight migration tests pass.

Stop and write a finding if repo reality conflicts with the spec.

Hard exclusions:
no summaries, no K2Think, no AIRequestLog, no LLMProvider, no PromptRegistry, no vector index, no retry UI, no direct fetch from pages/components.

Checkpoint B.5 reconciliation decisions are now part of the active plan:
- `transcript_chunks.chunking_version` is the semantic source for `chunkerVersion`; do not add `chunker_version` during 4.4 unless a later schema checkpoint proves it necessary.
- The projection route is `GET /modules/{module_id}/sections/{section_id}/transcript-processing-status`.
- The repo has no explicit RQ retry configuration today; treat that as a Checkpoint E worker finding/risk, not a Checkpoint C blocker.
- Repo-real paths replace the pasted examples for implementation planning.

## Approach
The main agent will execute the session in checkpoints. Checkpoint A persists the spec and this plan only. Checkpoint B delegates six read-only discovery/review tasks to explorer subagents, waits for all reports, consolidates concrete repo findings, and stops before source implementation.

Checkpoint C implements only the C-lite processing-status projection and 4.3.5e regression. It does not start schema preflight, pgvector dependency work, model packaging, embedding worker work, summaries, retry UI, or vector indexing.

## Changes, file by file
- `knowledge/specs/stage-04/4.4-embeddings.md` — session spec, preserving the developer-provided Stage 4.4 content and recording the blocked preconditions.
- `knowledge/plans/stage-04/4.4-embeddings.md` — session plan, preserving the developer-provided main-agent/subagent checkpoint plan.
- `backend/app/platform/query/transcript_status.py` — add the read-model projection that derives keyed step state, overall state, counts, and safe failure fields from `Transcript` + `IngestionJob`.
- `backend/app/domains/transcripts/schemas.py` — add response DTOs for the projection.
- `backend/app/domains/transcripts/service.py` — add lecturer-only service function using existing section authorization and transcript-not-found behavior.
- `backend/app/api/routers/transcripts.py` — add `GET /modules/{module_id}/sections/{section_id}/transcript-processing-status`.
- `backend/tests/test_transcripts.py` — add API/projection/authz coverage for current chunked behavior.
- `frontend/src/lib/api/**` — regenerate generated client after the backend OpenAPI shape exists.
- `frontend/src/lib/api/wrapper.ts` — expose the projection through centralized auth recovery.
- `frontend/src/features/content/lecturer/TranscriptStatusBadge.tsx` — poll projection state through the wrapper.
- `tests/e2e/4.3.5e-transcript.spec.ts` and `tests/e2e/fixtures/db.mjs` — update regression proof to assert `overallState === "chunked"`.
- `knowledge/steps/stage-04/4.3.5e-regression-under-projection.md` — record the real regression output.

## Order of operations
1. Confirm the requested knowledge paths match repo conventions.
2. Persist the spec file.
3. Persist the plan file.
4. Spawn all six read-only explorer subagents.
5. Wait for all results.
6. Consolidate findings and stop before source edits.
7. For Checkpoint B.5, reconcile the spec/plan with repo-real findings.
8. For Checkpoint C, implement the backend projection and targeted tests.
9. Regenerate the OpenAPI client and update frontend polling through `api.transcripts`.
10. Update and run the 4.3.5e regression gate; record the result; stop.

## Test strategy
Checkpoint A/B are knowledge/discovery only. Verification for this first run is:
- Confirm both files exist at the requested paths.
- Confirm no product source files were edited.
- Confirm all six subagent reports were received and consolidated.

## Risks & mitigations
- Existing dirty worktree could be mistaken for this session's edits → preserve all pre-existing dirty files as user-owned and report only the two new 4.4 files as Checkpoint A edits.
- Spec paths differ from repo paths → use the repo-real paths listed in this plan for implementation.
- Spec says `chunker_version`, while repo already has `chunking_version` → use `chunking_version` as the semantic source.
- Spec route differs from current route convention → use `/modules/{module_id}/sections/{section_id}/transcript-processing-status`.
- The repo has no explicit RQ retry configuration → record as Checkpoint E finding/risk; do not block Checkpoint C.
- Debugging risk if embedding work starts before projection regression passes → stop after Checkpoint C.

## Open questions
- None for Checkpoint A/B. Later implementation checkpoints must stop if discovery finds spec-vs-repo conflicts.

---

# Original Developer Plan

# Stage 4.4 Embeddings Plan — Main Agent + Subagents

## Execution Model

The **main agent owns implementation**. Subagents are used for bounded discovery, review, and evidence gathering. Subagents must not make source edits unless explicitly reassigned by the main agent after a checkpoint decision.

Codex only spawns subagents when explicitly instructed, so the main agent must request them directly, wait for all results, consolidate findings, and then proceed. ([OpenAI Developers][2])

### Main Agent Responsibilities

The main agent is responsible for:

* Persisting the Stage 4.4 spec and this plan.
* Making final implementation decisions.
* Applying source changes.
* Running migrations, tests, type-checks, and E2E gates.
* Updating knowledge files.
* Resolving or recording findings.
* Ensuring no out-of-scope work enters 4.4.

### Subagent Responsibilities

Subagents support the main agent with focused, mostly read-only work:

* Repo mapping.
* Test/gate mapping.
* Migration/schema review.
* Worker/concurrency review.
* Frontend/API contract review.
* Scope/security review.
* Browser/E2E evidence gathering where useful.

Subagents must report concrete file paths, symbols, commands, risks, and recommended next steps. They must not produce speculative architecture rewrites.

---

## Summary

* Persist the pasted Stage 4.4 spec as:

```text
knowledge/specs/stage-04/4.4-embeddings.md
```

* Persist this plan as:

```text
knowledge/plans/stage-04/4.4-embeddings.md
```

* These documents define the implementation scope, but **4.4 remains blocked from embedding work until §3.1 regression sign-off is recorded**.
* First implement the approved §3.1 C-lite projection and rerun the 4.3.5e browser gate against:

```text
overallState === "chunked"
```

* Only after that, add local `sentence-transformers/all-MiniLM-L6-v2` embeddings for transcript chunks, with pgvector storage, provenance, worker-driven embed jobs, lecturer-visible `Embedded` status, and no search/summaries/AI infra.

---

## Subagent Workflow

### Checkpoint 0 — Read-Only Discovery Pass

Before source edits, the main agent must spawn read-only subagents in parallel and wait for all results.

Use built-in `explorer` agents or project custom read-only agents if available. Custom agents can be defined under `.codex/agents/` with required `name`, `description`, and `developer_instructions`, and can be configured as read-only through sandbox settings. ([OpenAI Developers][3])

Spawn these subagents:

```text
1. repo_mapper
2. test_mapper
3. migration_reviewer
4. worker_reviewer
5. frontend_contract_reviewer
6. scope_security_reviewer
```

### Subagent 1 — `repo_mapper`

**Mode:** read-only
**Goal:** map repo reality before implementation.

Tasks:

* Locate transcript models:

  * `Transcript`
  * `TranscriptSegment`
  * `TranscriptChunk`
  * `IngestionJob`
* Confirm current embedding column/type.
* Confirm whether `transcript_chunks.chunking_version` exists.
* Confirm current chunk worker and enqueue-after-commit pattern.
* Confirm current route conventions for module/section transcript endpoints.
* Confirm current lecturer transcript-status polling hook/component.
* Identify exact source paths likely touched.

Return:

```text
- confirmed files
- existing fields and enums
- route convention recommendation
- deviations from spec
- risks requiring findings
```

### Subagent 2 — `test_mapper`

**Mode:** read-only
**Goal:** map test and gate infrastructure.

Tasks:

* Locate existing 4.3.5e Playwright transcript gate.
* Identify where `status === "completed"` is asserted.
* Identify E2E fixtures for VTT/TXT transcript upload.
* Locate migration test patterns.
* Locate backend worker integration test patterns.
* Locate generated API client workflow.
* Locate direct-fetch/JWT-role scan commands.
* Confirm how lecturer/student separate browser contexts are created.

Return:

```text
- test files to update
- commands to run
- 4.3.5e regression plan
- 4.4 E2E fixture recommendation
- risks around test flakiness
```

### Subagent 3 — `migration_reviewer`

**Mode:** read-only
**Goal:** review pgvector/schema design before migration edits.

Tasks:

* Inspect Alembic conventions.
* Inspect DB naming conventions.
* Check current vector extension handling.
* Review feasibility of replacing `UserDefinedType` with `pgvector.sqlalchemy.Vector(384)`.
* Check whether old non-null embeddings can exist.
* Propose exact migration assertions:

  * vector extension exists
  * column is `vector(384)`
  * provenance check constraint
  * embed-job partial unique index
  * `chunking_version` reuse/backfill behavior

Return:

```text
- migration risk list
- recommended migration sequence
- exact DB assertions
- whether broad or embed-only unique index is safer
```

Mandatory rule:

```text
The active-job partial unique index must be scoped to embed jobs unless the main agent explicitly decides to broaden it.
```

Recommended shape:

```sql
CREATE UNIQUE INDEX ingestion_jobs_one_active_embed_per_transcript
ON ingestion_jobs (transcript_id, job_type)
WHERE job_type = 'embed'
  AND status IN ('queued', 'running');
```

### Subagent 4 — `worker_reviewer`

**Mode:** read-only
**Goal:** review worker/idempotency/concurrency design before implementation.

Tasks:

* Inspect current worker architecture.
* Confirm how jobs are queued and retried.
* Confirm whether retries reuse the same `IngestionJob` row.
* Confirm queue names and concurrency controls.
* Review enqueue-after-commit pattern.
* Review how safe failure messages are produced.
* Validate the planned per-row `embeddingInputHash` staleness logic.

Return:

```text
- worker implementation checklist
- retry/concurrency risks
- whether separate embedding lane fits current worker architecture
- required config vars
- recommended failure semantics
```

Mandatory rule:

```text
Embedding gets a separate worker queue/concurrency lane, but remains part of the existing transcript ingestion pipeline and uses IngestionJob lifecycle, enqueue-after-commit, status projection, and existing worker conventions.
```

Mandatory staleness rule:

```text
Do not use one global :current_input_hash.
For each chunk row:
1. compute expectedEmbeddingInputHash from the exact text sent to the encoder
2. compare row.embedding_input_hash to that expected hash
3. encode only stale rows
```

### Subagent 5 — `frontend_contract_reviewer`

**Mode:** read-only
**Goal:** review API/frontend integration before UI edits.

Tasks:

* Inspect generated API client setup.
* Inspect transcript status polling hook.
* Inspect current lecturer status UI.
* Inspect student section view to ensure no transcript internals are exposed.
* Confirm no direct `fetch()` pattern is introduced.
* Confirm 401/403 handling convention.

Return:

```text
- frontend files to update
- generated-client workflow
- no-direct-fetch risks
- expected UI states
- student visibility risks
```

Mandatory rule:

```text
Projection polling must use the generated API client or centralized wrapper.
No direct fetch calls from pages/components.
```

### Subagent 6 — `scope_security_reviewer`

**Mode:** read-only
**Goal:** guard authorization and scope boundaries.

Tasks:

* Review planned route:

  * `/modules/{module_id}/sections/{section_id}/transcript-processing-status`
* Confirm it matches current repo route conventions.
* Check assigned lecturer access.
* Check student wrong-role access.
* Check unassigned lecturer/cross-tenant behavior.
* Confirm no vectors, raw chunk text, stack traces, model internals, or chunk counts leak to student UI.
* Confirm no search, summaries, K2Think, `AIRequestLog`, vector index, or retry UI enters 4.4.

Return:

```text
- authz risk list
- expected 403/404 behavior
- out-of-scope risk list
- required negative tests
```

Mandatory rule:

```text
If existing content/transcript endpoints use a different 403/404 convention, 4.4 follows the existing convention unless an ADR changes it globally.
```

---

## Main-Agent Checkpoints

The main agent must execute in checkpoints. Do not skip ahead.

## Checkpoint A — Persist Spec and Plan

Tasks:

* Save the Stage 4.4 spec to:

```text
knowledge/specs/stage-04/4.4-embeddings.md
```

* Save this plan to:

```text
knowledge/plans/stage-04/4.4-embeddings.md
```

* Record that 4.4 remains blocked from embedding work until §3.1 regression sign-off is completed.

Subagents:

* None required.

Stop condition:

```text
If knowledge paths differ from repo conventions, stop and report before inventing new structure.
```

---

## Checkpoint B — Read-Only Discovery

Tasks:

* Spawn all six read-only subagents listed above.
* Wait for all results.
* Consolidate findings into a short implementation-readiness report.

Required output:

```text
- confirmed assumptions
- repo deviations
- required findings
- exact files likely touched
- implementation order
- blockers before source edits
```

Stop condition:

```text
If repo reality conflicts with the spec, write a finding and stop before source edits.
```

---

## Checkpoint C — C-lite Projection + 4.3.5e Regression

Main-agent tasks:

* Add or adapt the processing-status projection.
* Use route pattern:

```text
GET /modules/{module_id}/sections/{section_id}/transcript-processing-status
```

* This route deviation from the spec is intentional only if it matches existing API conventions.
* Record the route decision in the 4.4 report or ADR.
* Projection derives:

  * `overallState`
  * keyed `steps`
  * counts
  * failed step
  * safe failure message
* Update 4.3.5e E2E gate to assert:

```text
overallState === "chunked"
```

* Record regression result:

```text
knowledge/steps/stage-04/4.3.5e-regression-under-projection.md
```

Subagents:

* `frontend_contract_reviewer` reviews the projection response shape and frontend polling contract.
* `scope_security_reviewer` reviews authz behavior.
* `test_mapper` verifies the 4.3.5e regression gate update.

Projection response should use keyed steps:

```json
{
  "overallState": "embedded",
  "currentPhase": null,
  "steps": {
    "upload": { "status": "completed", "startedAt": "...", "completedAt": "..." },
    "parse":  { "status": "completed", "startedAt": "...", "completedAt": "..." },
    "chunk":  { "status": "completed", "startedAt": "...", "completedAt": "..." },
    "embed":  { "status": "completed", "startedAt": "...", "completedAt": "..." }
  },
  "segmentCount": 42,
  "chunkCount": 12,
  "embeddedChunkCount": 12,
  "failedStep": null,
  "safeFailureMessage": null,
  "updatedAt": "..."
}
```

Stop condition:

```text
Embedding work must not begin until the 4.3.5e regression under projection is green and recorded.
```

---

## Checkpoint D — Schema Preflight

Main-agent tasks:

* Add `pgvector` dependency.
* Enable vector extension through Alembic migration:

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

* Replace local `UserDefinedType` with:

```python
pgvector.sqlalchemy.Vector(384)
```

* Add embedding provenance fields if missing.
* Add DB check constraint:

```sql
CHECK (
  embedding IS NULL
  OR (
    embedding_model IS NOT NULL
    AND embedding_model_revision IS NOT NULL
    AND embedding_dimension = 384
    AND embedding_normalization = 'l2'
    AND embedding_version IS NOT NULL
    AND embedding_input_hash IS NOT NULL
  )
)
```

* Reuse existing `transcript_chunks.chunking_version` as the chunker-version provenance source.
* Do not add a duplicate `chunker_version` column unless repo inspection proves `chunking_version` does not exist.
* Add active embed-job partial unique index scoped to `job_type = 'embed'`.
* Run migration tests before worker implementation.

Subagents:

* `migration_reviewer` reviews migration diff and DB assertions.
* `worker_reviewer` reviews whether the partial unique index affects existing worker semantics.

Stop condition:

```text
Embedding worker implementation must not begin until schema preflight tests are green.
```

---

## Checkpoint E — Embedding Encoder + Worker

Main-agent tasks:

* Add internal `EmbeddingEncoder` abstraction.
* Add real `SentenceTransformersEmbeddingEncoder`.
* Add deterministic test encoder for unit/integration tests only.
* Vendor/package pinned model at build time.
* Model pin:

```text
sentence-transformers/all-MiniLM-L6-v2
revision: 1110a243fdf4706b3f48f1d95db1a4f5529b4d41
```

* Worker startup must verify:

  * `EMBEDDING_MODEL_PATH` exists
  * vendored model revision matches configured pinned revision
  * required model/tokenizer files exist
* Configure:

  * `EMBEDDING_MODEL_PATH`
  * `EMBEDDING_BATCH_SIZE`
  * `EMBEDDING_WORKER_CONCURRENCY`
  * `EMBEDDING_DEVICE=cpu`
* Implement embed job:

  * enqueue after chunk transaction commits
  * one active embed job per transcript
  * L2-normalized vectors
  * canonical JSON input hash
  * per-row staleness checks
  * `FOR UPDATE SKIP LOCKED`
  * batch commits
  * partial-failure resume
  * same-row retry attempts
  * safe failure message
* Do not enqueue summary jobs.
* Leave embed → summary seam documented but unwired.

Subagents:

* `worker_reviewer` reviews worker diff for race conditions, retry semantics, and idempotency.
* `migration_reviewer` checks provenance constraint still holds after worker writes.

Mandatory hash payload:

```json
{
  "chunkText": "<exact text sent to encoder>",
  "embeddingModel": "sentence-transformers/all-MiniLM-L6-v2",
  "embeddingModelRevision": "1110a243fdf4706b3f48f1d95db1a4f5529b4d41",
  "embeddingNormalization": "l2",
  "chunkerVersion": "<transcript_chunks.chunking_version>"
}
```

Stop condition:

```text
If chunks are empty, missing chunking_version, or exceed model limits without a recorded truncation decision, stop and write a finding.
```

---

## Checkpoint F — Frontend/API Client

Main-agent tasks:

* Regenerate OpenAPI TypeScript client:

```bash
bash scripts/generate-api-client.sh
```

* Add or update centralized wrapper method for projection.
* Update transcript polling to consume:

  * `overallState`
  * keyed `steps`
  * safe failure fields
* Lecturer UI shows:

  * parsing
  * chunking
  * embedding
  * Embedded
  * Failed
* Student UI exposes no:

  * embedding phase
  * chunk count
  * embedding metadata
  * vectors
  * chunk text
  * transcript internals
  * stack traces

Subagents:

* `frontend_contract_reviewer` reviews generated-client use and no-direct-fetch compliance.
* `scope_security_reviewer` reviews student UI exposure.

Stop condition:

```text
If frontend requires data not returned by the projection, stop and update the projection contract intentionally. Do not fake frontend state.
```

---

## Checkpoint G — Tests and Browser Gates

Main-agent tasks:

Run schema preflight tests first:

```text
- migration round trip
- vector type/dimension check
- provenance constraint check
- embed partial unique index check
- non-null old embedding fail-fast guard
- 384-vector round trip
```

Run backend tests:

```text
- embedding encoder
- hash stability/staleness
- projection
- API/authz
- worker integration
- concurrency/retry
- startup validation
- full docker compose exec backend pytest
```

Run frontend checks:

```bash
bash scripts/generate-api-client.sh
docker compose exec frontend npx tsc --noEmit
```

Also run existing scans for:

```text
- direct fetch outside wrapper/upload helper
- JWT role guessing
```

Run E2E stack with the real model:

```text
- rebuild Docker image with vendored model
- seed fixtures
- run 4.3.5e regression gate
- run 4.4 browser gate:
  lecturer upload → parse → chunk → embed → UI Embedded
- verify DB vectors/provenance separately
```

Run truncation assessment:

```text
Use pinned tokenizer over seeded/E2E chunks.
Record:
- max estimated word-piece count
- p95 estimated word-piece count
- count above 256 word pieces
- accepted/finding/resolution decision
```

Record result:

```text
knowledge/steps/stage-04/4.4-truncation-assessment.md
```

Subagents:

* `test_mapper` reviews command coverage and E2E evidence.
* `worker_reviewer` reviews concurrency/retry test results.
* `frontend_contract_reviewer` reviews browser/UI evidence.
* `scope_security_reviewer` reviews authz negative results.

Stop condition:

```text
Do not mark 4.4 FULLY VERIFIED unless real browser gate, backend tests, frontend type-check, generated client freshness, and knowledge updates are complete.
```

---

## Checkpoint H — Knowledge Closeout

Main-agent tasks:

Add or update:

```text
ADR — C-lite status projection and 4.3.5e terminal-state supersession
ADR — pgvector adoption + column type fix
ADR — embedding model policy: 384 dim, pinned revision, L2 normalization, model swap = migration
ADR — vector index deferral
knowledge/steps/stage-04/4.3.5e-regression-under-projection.md
knowledge/steps/stage-04/4.4-truncation-assessment.md
Architecture doc — worker model loading, cold start, offline fail-fast
Architecture doc — DB/vector/provenance behavior, only where source changed
Architecture doc — frontend status projection behavior, only where source changed
Stage 4.4 implementation report from git diff and real command output
```

Subagents:

* `scope_security_reviewer` performs final scope audit.
* `test_mapper` verifies command output is recorded accurately.
* Optional `reviewer` subagent performs final PR-style review.

Stop condition:

```text
If any finding is unresolved, 4.4 cannot be marked FULLY VERIFIED.
```

---

## Key Changes

### Status Projection and Regression

* Add:

```text
GET /modules/{module_id}/sections/{section_id}/transcript-processing-status
```

* Derive:

  * `overallState`
  * keyed `steps`
  * counts
  * failed step
  * safe failure fields
* Source data:

  * `Transcript`
  * `IngestionJob`
* Update 4.3.5e E2E gate to assert:

```text
overallState === "chunked"
```

* Record:

```text
knowledge/steps/stage-04/4.3.5e-regression-under-projection.md
```

### Schema and Provenance

* Add `pgvector`.
* Replace local `UserDefinedType` with:

```python
pgvector.sqlalchemy.Vector(384)
```

* Add missing embedding provenance fields.
* Add DB check constraint.
* Add active embed partial unique index.
* Reuse existing:

```text
transcript_chunks.chunking_version
```

as chunker-version provenance source.

Semantic mapping:

```text
Spec field chunkerVersion = DB/model field transcript_chunks.chunking_version
```

### Embedding Worker

* Add an embedding queue/concurrency lane separate from lightweight ingestion jobs.
* Keep it inside the existing transcript ingestion pipeline.
* Package pinned model into backend image at build time.
* Validate model path, revision, and required files before embedding worker accepts jobs.
* Implement:

  * `SentenceTransformersEmbeddingEncoder`
  * deterministic test encoder

### Pipeline Behavior

After chunk completion:

```text
chunk transaction commits
→ create/enqueue one embed job
→ embed stale non-empty chunks
→ persist vector + provenance
→ projection reports embedded
```

Required worker behavior:

```text
- L2 normalization
- canonical JSON input hashes
- row locking with FOR UPDATE SKIP LOCKED
- per-row staleness checks
- batch commits
- partial-failure resume
- same-row retry attempts
- safe failure messages
```

### Frontend/API Client

* Regenerate OpenAPI TypeScript client.
* Add centralized wrapper method for projection.
* Update transcript polling to use keyed projection.
* Show `Embedded` / `Failed`.
* Do not expose:

  * vectors
  * chunk text
  * counts to students
  * stack traces
  * internal metadata

### Knowledge Closeout

* Add ADRs for:

  * C-lite projection
  * pgvector/model policy
  * index deferral
* Record truncation assessment.
* Update worker/db/frontend architecture docs only where source changed.
* Write 4.4 report from:

  * `git diff`
  * real command output
  * E2E evidence

---

## Test Plan

### Schema Preflight

Run first:

```text
- migration round trip
- vector type/dimension check
- provenance constraint check
- partial unique index check
- non-null old embedding fail-fast guard
- 384-vector round trip
```

### Backend Tests

Run targeted tests for:

```text
- embedding encoder
- embedding hash
- projection
- API/authz
- worker integration
- concurrency/retry
- startup validation
```

Then run:

```bash
docker compose exec backend pytest
```

### Frontend Checks

Run:

```bash
bash scripts/generate-api-client.sh
docker compose exec frontend npx tsc --noEmit
```

Also run existing scans for:

```text
- direct fetch outside wrapper/upload helper
- JWT role guessing
```

### E2E

Use real model:

```text
- rebuild Docker image
- seed fixtures
- run 4.3.5e regression gate
- run 4.4 browser gate
```

Browser gate:

```text
lecturer upload
→ parse
→ chunk
→ embed
→ UI Embedded
```

DB verification is separate:

```text
- vectors exist
- vector dimension = 384
- provenance fields set
- embeddingInputHash set
- chunking_version used in hash payload
```

### Truncation Assessment

Use pinned tokenizer over seeded/E2E chunks.

Record:

```text
- max estimated word-piece count
- p95 estimated word-piece count
- count above 256 word pieces
- concrete resolution
```

Write:

```text
knowledge/steps/stage-04/4.4-truncation-assessment.md
```

---

## Assumptions

* §3.1 C-lite is approved before embedding work begins.
* Existing dirty knowledge-file consolidation is user-owned and preserved.
* Route uses current API pattern:

```text
/modules/{module_id}/sections/{section_id}
```

* `chunking_version` is reused instead of adding `chunker_version`.
* Model pin:

```text
sentence-transformers/all-MiniLM-L6-v2
revision: 1110a243fdf4706b3f48f1d95db1a4f5529b4d41
```

* Model behavior:

  * 384-dimensional output
  * default long-input truncation
* Runtime default:

  * CPU embedding
  * one embedding worker process
  * batch size configured by `EMBEDDING_BATCH_SIZE`
  * no vector index
  * no summary enqueue
  * no K2Think
  * no `AIRequestLog`
  * no retry UI

---

## Explicit Non-Goals

Do not implement:

```text
- semantic search
- assistant retrieval
- student-facing embedding output
- summary generation
- K2Think calls
- AIRequestLog
- LLMProvider
- PromptRegistry
- vector index
- lecturer retry UI
- transcript viewer
- embedding/chunk debug viewer
- broad ingestion-job uniqueness changes outside embed jobs
```

---

## Suggested Codex Prompt

Use this when sending the plan:

```text
Implement Stage 4.4 using the attached spec and plan.

Use subagents only as described in the plan. The main agent owns implementation and final decisions. Subagents are read-only unless explicitly reassigned.

Start with Checkpoint A and Checkpoint B only:
1. Persist the spec and plan.
2. Spawn the read-only subagents:
   - repo_mapper
   - test_mapper
   - migration_reviewer
   - worker_reviewer
   - frontend_contract_reviewer
   - scope_security_reviewer
3. Wait for all subagent results.
4. Consolidate findings.
5. Stop and report confirmed assumptions, deviations, blockers, and the exact next implementation checkpoint.

Do not edit source code beyond persisting the spec/plan until the discovery pass is complete and any spec-vs-repo conflicts are resolved.

Do not begin embedding worker implementation until:
- §3.1 C-lite approval is recorded,
- 4.3.5e regression under projection passes,
- schema preflight migration tests pass.

Stop and write a finding if repo reality conflicts with the spec.

Hard exclusions:
no summaries, no K2Think, no AIRequestLog, no LLMProvider, no PromptRegistry, no vector index, no retry UI, no direct fetch from pages/components.
```

This is the right split: the main agent does the work; subagents reduce blind spots. Do not let subagents parallel-edit this feature.

[1]: https://developers.openai.com/codex/subagents?utm_source=chatgpt.com "Subagents – Codex | OpenAI Developers"
[2]: https://developers.openai.com/codex/concepts/subagents?utm_source=chatgpt.com "Subagents – Codex | OpenAI Developers"
[3]: https://developers.openai.com/codex/subagents.md?utm_source=chatgpt.com "developers.openai.com"
