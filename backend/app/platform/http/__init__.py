"""HTTP boundary concerns shared across routers (Stage 12a).

`request_id` — per-request correlation id middleware + accessor.
`errors` — global exception handlers producing the consistent error envelope.
"""
